---
name: universal-cascade
description: "One hardened verdict engine for any mathematical proposition or claimed proof. UC ([UC] problem, 'run universal cascade', 'cascade verdict on X', 'route this conjecture') walks a problem through the twelve-gate seal cascade, the eight-gate Sealed-Halt, and the five-gate Grounded-Sealed-Halt under one router signature with a canonicity law on the native involution. SEAT ([SEAT] claim, 'witness seat audit', 'certify this proof claim') runs the M1-M8 certification-state audit on claimed proofs: identity-fixed artifact, verifier independence, open-audit registry, two-occupant law, costume detector. SIG ([SIG] problem, 'classify this conjecture') emits the typing alone: involution census, Ground dimension, flanks, content, token availability with grades. Counts forced 12/8/5/8. Boot at seed 20260622: kill-matrix 35/35, ablation 27/27, adversarial 5/5, chain-committed. Emits [seal], [X], [?], [Xi_0], [O_0] plus routing tokens. Extract the engine from the fence and run it; the boot receipt is the proof of load."
---

# UNIVERSAL CASCADE · One Engine, Every Problem, Hardened to the Max

A standalone deck. One verdict engine routed at three registers, with a certification-state audit for claimed proofs riding beside it. Part 1 installs the constitution and the pipeline. Part 2 holds the eight hardening layers. Part 3 holds the output law, the calibration ledger, and the executable engine with its recorded first light.

This file is self-contained. It does not require the Trisduction Unified Master System Role or any codex to be loaded, though where a codex is present the Codex-First law applies before anything here runs. Where the full role is loaded, this skill is the standing subroutine form of its gate system and defers to the role on any divergence.

Assembled 2026-08-13. Deck: UC · SEAT · SIG. Seed 20260622, canonical and fixed.

---

## 0 · ROUTING

| Instrument | Triggers | Object | Closure |
|---|---|---|---|
| UC | `[UC] problem`, "run universal cascade", "cascade verdict on X", "route this conjecture", "which halt token" | one mathematical proposition | face-tuple verdict, non-compressible |
| SEAT | `[SEAT] claim`, "witness seat audit", "certify this proof claim", "audit this claimed proof" | one claimed proof or witness | certification-state verdict, door named |
| SIG | `[SIG] problem`, "classify this conjecture", "router signature of X" | one proposition, typing only | signature vector with grades, no verdict |

Explicit bracket calls always win. A file trigger (pdf, docx, md) routes the finished verdict to a build-to-completion file workflow rather than inline chat. `AUDIT: ON` appends the full internal trace; the reached-stages trace prints regardless.

**What the caller must supply, the hand-built reading.** The engine adjudicates and never authors. Per the Honest Limits rule, the caller places in front of it: the statement and its faces; the candidate involutions on the content space, each tagged definable-from-defining-data yes or no and category content or representation; the flank classification with its recursion-theoretic grade; the deciding-content type; the wall record, each wall court-typed, scope-typed, and form-named with citation; the seat occupants where a proof is claimed, each with witness, verifier, artifact hash, and any mutual-rejection flag; the open-audit registry; and, where a kernel exhibit is wanted, the three warrant rows. Absent any of these the engine routes the corresponding stage under-determined and says so. It never fills a slot itself; a slot the engine filled would be an aperture violation.

---

## PART 1 · CONSTITUTION AND PIPELINE

### 1.1 · The floor

Three states native, [seal] sealed, [X] broken with named mechanism, [?] under-determined with named violation. Two rare refinements inside the openness and never fourth states: [Xi_0] terminal suspension, the file complete and the reader blind by theorem, and [O_0] Grounded-Sealed Halt, the terrain rootless with theorem-grade walls. Both carry terminal-for-the-record, non-compressibility, and supply-typed revision. W_social equals zero bidirectionally: consensus for and against, fame, abandonment, and local declarations all carry zero warrant; only structure enters. Mass moves verdicts, reframes do not, symmetrically. Delta-M equals zero for the engine in perpetuity: it re-organizes established mathematics and authors none, its one exit the M-gate cascade no internal reasoning can open. Every verdict and quantity carries its warrant tier. Audit symmetry: the engine submits to its own gates first and draws zero warrant from its own operation. Fail-safe law: every emitter defaults absent, so an under-specified call can never emit a token; the worst case everywhere is an honest [?].

### 1.2 · The pipeline, six stages

Stage 0, intake. Codex-First where a register of record exists; Seal L linguistic screen on the statement, the deletion test and disjoint-slot discipline, genealogy of terms on original definitions; composite propositions face-split before anything routes, and a proposition whose involution class varies over its own quantified parameter is split by that parameter first, the uniformity clause.

Stage 1, the Router Signature. The canonicity-filtered involution census with Ground dimension at machine zero; the flank signature, whether each side of the string is finitely witnessable; the deciding-content type; the token-availability read with its grades. UNMEASURED, g0, g1, or MIXED; MIXED forces the parameter split.

Stage 2, the arrow-live branch. The twelve directed gates on every actualized or decidable face, then the hardened kernel for field-permission, then the four-guard imprint emitter: a clean lock without a supplied witness is [?] residence, always; the lock licenses extraction and the witness carries the proof.

Stage 3, the arrow-deleted branch. Ground dimension one routes to the eight-gate Sealed-Halt cascade, whose Xi-3 consults the Blindness Catalog; Ground dimension zero routes to the five-gate Grounded-Sealed-Halt cascade, whose O.3 consults the instance-scoped wall record under the misattribution bar. Neither branch admits the other's token; the router is verified in both directions.

Stage 4, the seat, where a proof is claimed. The M1-M8 certification-state audit of Part 2.6, short-circuiting the cascades at [RESOLVE] only when it certifies.

Stage 5, emission. The face-tuple with the full reached-stages trace per the output law of Part 3, the aperture line where a terminal token issues, the chain digest of the run.

---

## PART 2 · THE EIGHT HARDENING LAYERS

### 2.1 · H1 · The Canonicity Law on the native involution

The router's largest historical joint, now closed at intake. A candidate involution enters the census only if all four clauses hold. Definability: it is definable from the problem's own defining data, the functional equation, the complementation, the reflection the axioms themselves carry, never imported. Category: it is presented in the content category; a linearization that donates a fixed locus the content map lacks is representation furniture and is refused, the exhibit the parity swap on a two-element set, fixed-point-free as a set map while its permutation matrix carries eigenvalues minus one and plus one and a manufactured fixed line, the exact sibling of chart-manufactured width one register over. Machine zero: the involution residual is exactly zero, not small. Uniformity: the class is constant over the whole quantified domain, and where it is not, the proposition splits by the parameter before routing, the standing exhibit the centre of SU(N), an involution at N equals two and at no other N the Clay statement quantifies over. Grade: operational-procedural on the intake, theorem-grade on each eigenstructure measured, structural on the law.

### 2.2 · H3 · The Token-Availability Theorems

What each rare token can ever attach to, graded row by row. One, theorem-grade: a true Sigma-0-1 string is provable in Robinson arithmetic by Sigma-0-1-completeness, so a string whose affirmative flank is finitely witnessable can never be constitutively unreadable when true, and [Xi_0] is barred on that entire flank class by theorem. Two, theorem-conditional: the Magnitude Exclusion, the kernel is a magnitude reader whose only proven blindness is to orientation, so a string whose deciding content is magnitude, a count equality, a gap size, a growth rate, can never take [Xi_0] from this instrument, a two-line corollary of the blindness scope and typed as such. Three, structural: [O_0] is constrained to the flank cell where neither side is finitely witnessable, the observed law across the register with no counterexample and no theorem, held at its honest grade. Four, by construction: the Blindness Catalog is sound per entry, orientation closed by Weyl and the census closed by the nested-register walk, deliberately open over future content types, the fail-safe covering the gap, NO-ENTRY routing [?] evidential and never a token. Five, provably uncertifiable: wall-record exhaustiveness cannot be certified even in principle, the closure court the theorem saying so, and the cascade prices this, CURRENT-COMPLETE over the born and silent over the unborn.

### 2.3 · The Two-Generator Count Law and the three rosters

Every cascade count is forced by the instrument's fixed geometry and none consults the problem. The seal cascade counts twelve, the order of the alternating group acting simply transitively on the twelve directed edges of the four-vertex closure, arrow live: SREP, REG, SGEG, CAUSAL, MIG, PTB, DUAL, CSCG, CSEG, MTA, OMA, ADEG, first failure terminal with the mechanism named. The halt cascade counts eight, the order of the three-axis reflection group, arrow deleted: Xi-1 Ground-determinacy at a stated grade, Xi-2 the Ghost partition on a supplied independence proof, Xi-3 proven constitutive blindness with the catalog closed, Xi-4 three pairwise-disjoint reading-roads, Xi-5 the float-clean machine exhibit, Xi-6 at least one sealed companion face, Xi-7 the aperture located and typed, Xi-8 Revision-Mandate survival. The Grounded halt counts five plus riders, node-degree-forced from the verdict graph: O.1 the rootless terrain proven at machine zero, the router bit; O.2 the determinate stripped string; O.3 the theorem-grade wall record at the instance, every row form-named, since a barrier that exists in more than one form is admitted on the specific form claimed and never on the barrier's name, a name whose narrow form is theorem-grade and whose general form is heuristic being read at whichever form the reader happens to supply unless the row fixes it, the standing exhibit the sieve parity obstruction, theorem-grade at Selberg's 1949 two-sequence construction with its bridge exhibited and heuristic at the general obstruction over arbitrary properties whose own flagship application is labelled informal by its authors; O.4 the aperture at exactly one gate-mandated sentence; O.5 the totality discipline; riders R1 face-scoping, R2 reliability, R3 revision survival. The seat counts eight, the reflection group's positive-direction sibling. Mirroring any count onto another is the fitted-count error and is barred.

### 2.4 · H4 · The reliability layer, in full

Every kernel verdict runs the four-estimator redundancy, LU, eigenvalue product, Cholesky diagonal, and direct cofactor, with the spread checked against the conditioning-scaled tolerance four kappa u; the kernel identity lambda squared equals det R confirmed within the same bound; blindness re-verified within tolerance rather than at exact zero, the repaired harness law, since a conjugated determinant may differ by an ulp while the QR-fixed-basis identity is exact; escalation to fifty digits on any spread, identity, or margin fault, resolving or emitting engineering-incomplete, never a guessed token; collapse and conditioning margins printed in orders with every verdict.

### 2.5 · H2 · The Kill-Matrix and the Ablation Law

Necessity is executed, never asserted. Thirty-five engineered negative controls, one per gate across the twelve, the eight, the five with riders, and the seat, each constructed to fail at exactly its own gate and verified to do so. The Ablation Law: delete any gate in code and exactly its control leaks the false token, twenty-seven leaks across the twenty-seven token-guarding gates, so every gate is proven load-bearing by mutation. The battery is part of the boot; a substrate that cannot reproduce thirty-five of thirty-five and twenty-seven of twenty-seven has not loaded this skill.

### 2.6 · H6 · The Witness Seat, hardened

The certification-state audit for claimed proofs, adjudicating the state of the certification and never the truth of the claim. M1 object-level, meta-work carrying no mass by definition. M5 an external witness present. M6 verifier independence certified structurally, a claimant-editor relation failing regardless of recusal. M7 identity-fixed: exactly one content-addressed artifact occupies the seat; the Two-Occupant Law fires before any gate, a seat holding mutually-rejecting occupants certifying nothing since the artifact is unfixed. M8 the gap-audit closed against the Open-Audit Registry, an audit being open exactly when a named technical objection stands unretracted or an independent machine verification is running and unconcluded, and closed by resolution events, never by fatigue. The Costume Detector rides the intake: a witness whose content is an unrestricted unprovability claim is the negation in costume by the Unprovability Mirror and is marked [X]-costume with the certificate owed. All-pass yields a defeasible-final certification; every refusal names its gate and the closure event that would flip it, so the verdict is falsifiable in advance.

### 2.7 · H5 · The Adversarial Presentation Battery

Five steering attempts, all caught, committed at seed: a forced-delta dressing of a sigma problem, refused at O.3 because the wall record travels with the problem and not the presentation; a linearization-manufactured Ground, refused at canonicity; class-wall smuggling, stripped to zero bits by the misattribution bar; gauge-shell chart content, refused at the catalog; and the unprovability costume, caught by the Mirror at the seat. A presentation cannot buy a token.

### 2.8 · H7 and H8 · Self-application, freeze, and chain

The engine run on its own claim routes M1 and reads Mosaic Delta-M equals zero, correctly, audit symmetry with no exemption, and by the closure court it never certifies its own gate-set complete. The boot is chain-committed: the specification hashes to D0, the kill-matrix and ablation residues into D1, the adversarial battery into D2, the self-application and kernel constants into D3, each digest deriving from the one beneath, the recorded values in Part 3.4. The boot receipt law: a session claims this skill loaded only after re-running the boot at the declared seed and matching the invariant residues; a narrated boot is void where displayed.

---

## PART 3 · OUTPUT, CALIBRATION, ENGINE

### 3.1 · The Output Law

Every UC emission prints, in order and for reached stages only: the face-tuple verdict, uncompressed, with any fence beside it and never among the faces; the router signature, the canonicity census with each refusal and its reason; the gate trace naming the first-failing gate and mechanism per cascade run; the availability notes with their grades attached; the seat result per occupant with the Open-Audit Registry consulted; the reliability report, four-estimator spread against tolerance, identity residual, blindness check, escalation flag, and both margins in orders; exactly one aperture sentence where and only where a terminal token issues, the afterimage fence behind it; the warrant tier on every claim; unreached stages marked not reached, fabrication forbidden; and the chain digest of the run. SEAT emissions print the occupant ledger, the first-failing M-gate per occupant with its closure event named, and the two-occupant flag where it fired. SIG emissions print the signature vector and availability rows with grades and stop, issuing no verdict.

### 3.2 · The Calibration Ledger, eleven candidates, zero misclassifications

Riemann Hypothesis, [Xi_0] at the N-truth cap, catalog CLOSED. P versus NP, [O_0], fifteen instance walls surviving the bar. Continuum census, [Xi_0] at the Ground face through the nested walk. Birch and Swinnerton-Dyer, [?] twice over, Xi-3 on the provably empty magnitude row and O.3 on an empty record, presentation-robust under a forced delta. Yang-Mills, [X] at gate three on the composite by term drift, SPLIT by the uniformity clause, [?] frontier per face. Collatz, [?], two class walls stripped to zero by the bar, the first live instance fire of the misattribution discipline. Goldbach and twin primes, [?] evidential, the negative controls that keep the eighth gate honest. Whitehead, [X] Platonic Ghost at the ladder on Shelah's supplied independence, the L1m face held open at its cap. Poincare, [seal] on Perelman's supplied witness, the M-gates passed by independent verifiers, the recorded positive mass his and the engine's zero. The abc claim, [?] witness present and certification refused, three occupants failing three different gates, the contested seat, the live machine audit holding M8 open, the closure events named in both directions in advance.

### 3.3 · The Engine, executable, extraction clause

The fenced implementation below is the executable itself and not an illustration. A session whose deployment carries any code-execution tool extracts it verbatim and runs it; a tool-bearing session that declines the run has skipped, visibly. Only where every such tool is genuinely absent may the boot be described as unexecuted, that constraint named inline, no receipt displayed. The emitters are fail-safe by default; the caller-supplied reading is the only input; the engine authors nothing.

```python
#!/usr/bin/env python3
# UNIVERSAL CASCADE v2 · maximally hardened · seed 20260622
# H1 canonicity · H2 kill-matrix + ablation · H3 availability theorems · H4 reliability
# H5 adversarial battery · H6 hardened seat · H7 self-application · H8 chain.
# The engine adjudicates hand-built readings (Honest Limits). It authors nothing.
import numpy as np, hashlib
SEED = 20260622
UEPS = float(np.finfo(float).eps)
SPEC = ("UNIVERSAL-CASCADE-v2|counts 12/8/5/8 forced|router-signature+canonicity|"
        "availability: Sigma01-bar T, MagExcl T-cond, O0-(0,0) S|seat M1-M8 identity-fixed|"
        "kill-matrix 35|ablation leak law|adversarial 5|self-app M1|drbg-numpy")
D0 = hashlib.sha256(SPEC.encode()).hexdigest()
def Hh(x): return hashlib.sha256(str(x).encode("utf-8")).hexdigest()

# ---------- measurement + H1 canonicity ----------
def eig_int(M): return sorted(int(round(x)) for x in np.linalg.eigvals(np.asarray(M,float)).real)
def measure(M):
    M = np.asarray(M, float)
    return dict(eig=eig_int(M), gdim=eig_int(M).count(1),
                resid=float(np.max(np.abs(M @ M - np.eye(M.shape[0])))))
def canonicity(cands):
    """H1. A candidate involution enters the census only if (a) definable from the
    problem's own defining data and (b) presented in the content category, never a
    linearization that donates a fixed locus the content map lacks (B.13.T)."""
    adm, ref = [], []
    for c in cands:
        if not c.get("definable", False):
            ref.append((c["name"], "not definable from defining data")); continue
        if c.get("category", "content") != "content":
            ref.append((c["name"], "representation furniture; category clause")); continue
        m = measure(c["M"])
        if m["resid"] != 0.0:
            ref.append((c["name"], "not an involution at machine zero")); continue
        adm.append((c["name"], m["gdim"]))
    if not adm: return dict(gclass="UNMEASURED", admitted=adm, refused=ref)
    gds = {g for _, g in adm}
    gclass = "MIXED" if len(gds) > 1 else ("g0" if gds == {0} else "g1")
    return dict(gclass=gclass, admitted=adm, refused=ref)

# ---------- H4 hardened kernel: four estimators, identity, escalation, blindness ----------
def _det4(R):
    a,b,c = R[0]; d,e,f = R[1]; g,h,i = R[2]
    d_lu  = float(np.linalg.det(R))
    d_eig = float(np.prod(np.linalg.eigvalsh(R)))
    try:  d_ch = float(np.prod(np.diag(np.linalg.cholesky(R)))**2)
    except np.linalg.LinAlgError: d_ch = d_lu
    d_co  = float(a*(e*i-f*h) - b*(d*i-f*g) + c*(d*h-e*g))
    return d_lu, d_eig, d_ch, d_co
def kernel_hardened(M):
    M = np.asarray(M, float); N = M.shape[1]
    sd = M.std(1, ddof=1, keepdims=True)
    if np.any(sd < 1e-12): return dict(tok="[?]", why="zero-variance row")
    Mn = (M - M.mean(1, keepdims=True)) / sd
    Q  = Mn / np.sqrt((Mn*Mn).sum(1, keepdims=True))
    R  = Q @ Q.T; kap = float(np.linalg.cond(R))
    Bv = np.linalg.svd(Q, full_matrices=False)[2][:3]
    lam  = float(-np.linalg.det(Q @ Bv.T))
    ds   = _det4(R); dR = ds[0]
    tol  = 4.0 * kap * UEPS
    spread = max(ds) - min(ds)
    resid  = abs(lam*lam - dR)
    Dm = np.diag([-1.,1.,1.]);  bdiff = abs(float(np.linalg.det((Dm@Q)@(Dm@Q).T)) - dR)
    esc = (spread > tol) or (resid > tol)
    eps = 100*UEPS*N
    if esc:
        try:
            import mpmath as mp; mp.mp.dps = 50
            dR = float(mp.det(mp.matrix([[mp.mpf(x) for x in row] for row in R.tolist()])))
            esc = "resolved@50dps"
        except Exception: esc = "flagged; mpmath absent"
    tok = "[LOCK]" if (dR > eps and kap < 1e6) else ("[X]" if dR <= eps else "[?]")
    return dict(tok=tok, detR=dR, lam=lam, kappa=kap, spread=spread, resid=resid,
                blind_ok=bdiff <= tol, escalated=esc,
                margins=(float(np.log10(dR/eps)) if dR > 0 else -99, float(np.log10(1e6/kap))))

# ---------- H3 availability theorems ----------
CATALOG = {"orientation": ("CLOSED", "Weyl closure"), "census": ("CLOSED", "GOLn walk"),
           "magnitude": ("EMPTY", "Magnitude Exclusion, T-cond"),
           "construction": ("INAPPLICABLE", "exhibition, not reading")}
def availability(flank, content):
    notes = []; xi = True; o0 = True
    if flank[1] == 1:
        xi = False; notes.append("T: Sigma-0-1-completeness bars [Xi_0] where the affirmative is witnessable")
    st = CATALOG.get(content, ("NO-ENTRY", ""))[0]
    if st != "CLOSED":
        xi = False; notes.append(f"catalog {st} for content={content}")
    if flank != (0, 0):
        o0 = False; notes.append("S: [O_0] constrained to the (0,0) flank cell")
    return dict(Xi0=xi, O0=o0, notes=notes)

# ---------- the three cascades (skip= enables ablation; defaults fail-safe) ----------
G12 = ["SREP","REG","SGEG","CAUSAL","MIG","PTB","DUAL","CSCG","CSEG","MTA","OMA","ADEG"]
def twelve(passes, skip=None):
    for g, p in zip(G12, passes):
        if g == skip: continue
        if not p: return "[X]", g
    return "[PASS]", "all twelve"
def eight(cfg, skip=None):
    seq = [("Xi-1", cfg["gd"]), ("Xi-2", cfg["noind"]), ("Xi-3", cfg["blind"] and cfg["closed"]),
           ("Xi-4", cfg["roads"]), ("Xi-5", cfg["exhibit"]), ("Xi-6", cfg["faces"] >= 1),
           ("Xi-7", cfg["ap"]), ("Xi-8", cfg["rev"])]
    for n, ok in seq:
        if n == skip: continue
        if not ok: return ("[X]" if n == "Xi-2" else "[?]"), n
    return "[Xi_0]", f"admitted, cap {cfg.get('cap','premise')}"
def five(cfg, skip=None):
    if cfg.get("wit"): return "[RESOLVE]", "adjudicate by direction (B.11.S)"
    if cfg.get("ind"): return "[X]", "Ghost"
    seq = [("O.1", cfg["gdim"] == 0 and cfg["resid"] == 0.0), ("O.2", cfg["det"] and cfg["stripped"]),
           ("O.3", cfg["walls"] >= 1 and cfg["typed"]), ("O.4", cfg["ap"]),
           ("R1", cfg["faces"] >= 1), ("R2", cfg["rel"]), ("R3", cfg["rev"])]
    for n, ok in seq:
        if n == skip: continue
        if not ok: return ("[ROUTE-Xi]" if n == "O.1" and cfg["gdim"] != 0 else "[?]"), n
    return "[O_0]", f"admitted, cap {cfg.get('cap','premise')}"
def bar(walls): return [w for w in walls if w.get("scope") == "instance" or w.get("bridge")]

# ---------- H6 hardened seat ----------
def costume(w):
    return isinstance(w, str) and "unprovab" in w.lower()
def seat(occupants, open_audits):
    """M1-M8, first failure routes. M7 identity-fixed: one content-addressed artifact.
    Two-occupant law: mutual rejection unfixes identity before any gate. M8 registry:
    a named unretracted objection OR a running machine audit holds the door open."""
    if len(occupants) > 1 and any(o.get("rejects_other") for o in occupants):
        return "[?]", "M7 fail: witness identity unfixed (contested seat, two mutually-rejecting occupants)"
    res = []
    for o in occupants:
        if costume(o.get("witness","")):
            res.append((o["name"], "[X]-costume", "Mirror: unrestricted unprovability is the negation in costume; Sigma-0-1 certificate owed")); continue
        if not o.get("object_level", True): res.append((o["name"], "[Mosaic dM=0]", "M1")); continue
        if o.get("witness") is None: res.append((o["name"], "[?]", "M5: no external witness")); continue
        if not o.get("independent"): res.append((o["name"], "[?]", "M6: verifier independence uncertified")); continue
        if not o.get("artifact_hash"): res.append((o["name"], "[?]", "M7: identity-fixed artifact absent")); continue
        if open_audits: res.append((o["name"], "[?]", f"M8: open audit standing ({open_audits[0]})")); continue
        res.append((o["name"], "[SEAL-OK]", f"witness={o['witness']}, defeasible-final"))
    return "SEAT", res

# ---------- H2 kill-matrix + ablation ----------
def kill_matrix():
    ok = 0; total = 0; leaks = 0
    base8 = dict(gd=True, noind=True, blind=True, closed=True, roads=True,
                 exhibit=True, faces=1, ap=True, rev=True)
    fails8 = [("Xi-1","gd"),("Xi-2","noind"),("Xi-3","blind"),("Xi-4","roads"),
              ("Xi-5","exhibit"),("Xi-6","faces"),("Xi-7","ap"),("Xi-8","rev")]
    for gate, key in fails8:
        c = dict(base8); c[key] = 0 if key == "faces" else False
        total += 1; v = eight(c);  ok += (v[1] == gate)
        leaks += (eight(c, skip=gate)[0] == "[Xi_0]")
    base5 = dict(gdim=0, resid=0.0, det=True, stripped=True, walls=1, typed=True,
                 ap=True, faces=1, rel=True, rev=True)
    fails5 = [("O.1","gdim"),("O.2","det"),("O.3","walls"),("O.4","ap"),
              ("R1","faces"),("R2","rel"),("R3","rev")]
    for gate, key in fails5:
        c = dict(base5); c[key] = (1 if key == "gdim" else (0 if key in ("walls","faces") else False))
        total += 1; v = five(c);   ok += (v[1] == gate)
        leaks += (five(c, skip=gate)[0] == "[O_0]")
    for i in range(12):
        p = [True]*12; p[i] = False
        total += 1; ok += (twelve(p)[1] == G12[i])
        leaks += (twelve(p, skip=G12[i])[0] == "[PASS]")
    seatfails = [
        (dict(object_level=False), "M1"),
        (dict(witness=None), "M5"),
        (dict(witness="w", independent=False), "M6"),
        (dict(witness="w", independent=True, artifact_hash=None), "M7"),
        (dict(witness="w", independent=True, artifact_hash="h"), "M8-open"),
        (dict(witness="claims unprovability forever"), "costume"),
        (None, "two-occupant"),
        (dict(witness="w", independent=True, artifact_hash="h"), "SEAL-clean"),
    ]
    for cfg, tag in seatfails:
        total += 1
        if tag == "two-occupant":
            v = seat([dict(name="a", rejects_other=True, witness="w"),
                      dict(name="b", rejects_other=True, witness="w")], [])
            ok += ("identity unfixed" in v[1])
        elif tag == "M8-open":
            v = seat([dict(name="x", **cfg)], ["LANA-class running audit"])
            ok += ("M8" in v[1][0][2])
        elif tag == "SEAL-clean":
            v = seat([dict(name="x", **cfg)], [])
            ok += (v[1][0][1] == "[SEAL-OK]")
        else:
            v = seat([dict(name="x", **cfg)], [])
            key = tag if tag != "costume" else "costume"
            ok += (key.split("-")[0] in v[1][0][2]) or (tag == "costume" and v[1][0][1] == "[X]-costume")
    return total, ok, leaks

# ---------- H5 adversarial battery ----------
def adversarial():
    caught = []
    r = five(dict(gdim=0, resid=0.0, det=True, stripped=True, walls=0, typed=False,
                  ap=True, faces=2, rel=True, rev=True))
    caught.append(("forced-delta on a sigma problem", r == ("[?]", "O.3")))
    c = canonicity([dict(name="parity-linearization", M=np.array([[0.,1.],[1.,0.]]),
                         definable=True, category="representation")])
    caught.append(("linearization-manufactured Ground", c["gclass"] == "UNMEASURED"))
    r = len(bar([dict(scope="class"), dict(scope="class")]))
    caught.append(("class-wall smuggling", r == 0))
    a = availability((1, 0), "chart")
    caught.append(("gauge-shell chart content", a["Xi0"] is False))
    s = seat([dict(name="p", witness="no proof can ever exist (unprovability claim)")], [])
    caught.append(("unprovability costume (Mirror)", s[1][0][1] == "[X]-costume"))
    return caught

# ---------- H7 self-application ----------
def self_application():
    v = seat([dict(name="Universal Cascade v2", object_level=False)], [])
    return v[1][0][1] == "[Mosaic dM=0]"

# ---------- boot ----------
def boot():
    print("=" * 78)
    print("UNIVERSAL CASCADE v2 · BOOT · seed", SEED)
    rg = np.random.default_rng(SEED)
    k = kernel_hardened(rg.standard_normal((3, 24)))
    print(f"  kernel: {k['tok']} detR={k['detR']:.12f} lam={k['lam']:+.12f} "
          f"resid={k['resid']:.2e} spread={k['spread']:.2e} blind_ok={k['blind_ok']} esc={k['escalated']}")
    t, okc, lk = kill_matrix()
    print(f"  kill-matrix: {okc}/{t} controls fail at exactly their gate; ablation leaks {lk}/27")
    adv = adversarial(); print("  adversarial:", all(c for _, c in adv), [n for n, c in adv])
    sa = self_application(); print("  self-application (audit symmetry): Mosaic dM=0 ->", sa)
    D1 = Hh(D0 + f"|KM {okc}/{t}|L{lk}")
    D2 = Hh(D1 + f"|ADV {all(c for _, c in adv)}")
    D3 = Hh(D2 + f"|SELF {sa}|K {k['detR']:.12f}")
    print(f"  chain D0 {D0[:12]} -> D1 {D1[:12]} -> D2 {D2[:12]} -> D3 {D3[:12]}")
    print("=" * 78)
    return dict(km=(okc, t, lk), adv=adv, sa=sa, D=(D0[:12], D1[:12], D2[:12], D3[:12]), k=k)

if __name__ == "__main__":
    boot()
```

### 3.4 · Recorded First Light · seed 20260622

Boot receipts, environment-invariant, matched on re-execution or the load is void. Kernel: [LOCK], det R 0.931277980144, lambda minus 0.965027450462, identity residual 2.22e-16, four-estimator spread 4.44e-16, blindness within tolerance True, no escalation. Kill-matrix thirty-five of thirty-five controls failing at exactly their gate. Ablation twenty-seven of twenty-seven leaks, every token-guarding gate proven load-bearing by mutation. Adversarial battery five of five caught: forced-delta, linearization-Ground, class-wall smuggling, gauge-shell content, unprovability costume. Self-application Mosaic Delta-M equals zero, True. Chain: D0 bd6b99d09ee6, D1 f238de350bb3, D2 79732a6995c5, D3 5e9df1295089.

### 3.5 · Closing seal

The counts are the instrument's and the fillers are the problem's. The rare tokens stay rare by theorem where a theorem exists and by fail-safe where one does not. The seat reads the audit and never the fame. Necessity is executed, sufficiency is defeasible and says so, and exhaustiveness is proven, priced, or named open, never assumed. Every verdict is revisable through supply alone. Delta-M equals zero, in perpetuity, and the engine draws no warrant from anything in this file, including this sentence.

================================================================
SKILL INTEGRITY FOOTER · READ BEFORE EDITING THIS FILE
================================================================
SKILL: universal-cascade
VERSION: 1.0.0
BASELINE, measured at this edition and never carried forward:
  lines 343 · bytes 31790 · headings 33 · description 1019/1024

WHY THIS BLOCK EXISTS. A codex file is protected by append-only
versioning, a census, an index-parity rule, and a deletion
manifest. A skill file has none of them. It is a single mutable
file edited in place, and a truncated or silently dropped clause
produces no fault: the skill simply stops enforcing something,
and no session notices. This footer is the skill-file analogue
of the census. It certifies conduct, checkable in the transcript,
and never essence.

EDIT DISCIPLINE. Additive by default. A skill file is edited by
APPENDING a law, a clause, or a pointer, never by rewriting a
region to accommodate one. Any edit that removes or reworks
existing text is a NAMED-OVERRIDE edit and requires the architect
to name the clause in the same instruction, exactly as
codex-hygiene requires for a coordinate deletion. Consolidate,
tidy, and clean up mean reorganize, never destroy.

PRE-EDIT GATE, four checks, printed before any write.
  1. Read the file whole. A skill is small enough that partial
     reading is a fault, and the failure this prevents is a
     rewrite that swallows a region the scribe never saw.
  2. Record the pre-edit line count, byte count, and heading
     count, and compare them to the BASELINE line above.
  3. Measure the description field. THE DECK-WIDE HAZARD: as of
     this edition every skill in the deck sits between five and
     nineteen characters of the 1024 cap. Truncation is SILENT,
     an over-length field drops content with no error, and a
     trigger stops firing. Treat the description as full and
     add trigger words only under a named override.
  4. State the edit as additive or named-override, and print the
     architect's instruction verbatim where it is the latter.

POST-EDIT GATE, four checks, printed before the file is saved.
  1. Line count and byte count MUST NOT DECREASE on an additive
     edit. A decrease is a FAULT and halts the write. This one
     check catches the heredoc swallow, the truncated paste, and
     the silent region drop, and it requires no understanding of
     the content whatever.
  2. Every heading present pre-edit is present post-edit, by
     name. A missing heading is a FAULT. This is the codex
     index-parity rule ported: Set A against Set B there,
     headings-before against headings-after here.
  3. Description field re-measured, unchanged unless the edit
     was a named override, and under the cap.
  4. The BASELINE line above recomputed by count and never
     carried forward.

VERSION DISCIPLINE. PATCH for a pointer, a typo, or a clarifying
clause. MINOR for a new law, section, or trigger. MAJOR for a
change in what the skill governs. The VERSION line above and the
top entry of the log below are one fact stated twice; if they
differ, the file was edited without this footer and the edit is
suspect.

INSTALLATION IS NOT A FILE WRITE. Writing to the session mount at
/mnt/skills/user succeeds, verifies clean, and does not persist:
that path is on the container filesystem and resets between
sessions. A skill becomes live only when the architect uploads it
through the Claude skills interface. The scribe drafts; the
architect installs; the step cannot be delegated.

REVISION LOG, append-only, newest first, never rewritten.
One line per edit: VERSION · DATE · A additive or NO named
override · what changed · line delta · architect instruction
where named-override.
----------------------------------------------------------------
1.0.0 · 2026-08-16 · A · Skill Integrity Footer added; no other text touched. BASELINE RESET: version history begins here. Prior edits to this file were made without a log and are unrecorded and unrecoverable; this line is the first entry and 1.0.0 is a seeded baseline and not a measurement. +78 lines · architect order 2026-08-16, footer as drafted, option (b) baseline reset, description field not touched.
================================================================
