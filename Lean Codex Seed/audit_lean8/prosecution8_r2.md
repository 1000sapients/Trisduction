# Prosecution · cycle lean8 · round 2 · registers: parameter, provenance
Artifact: lean8_r2_seeded.lean. Targets: the receipt block and its provenance, the pin, the toolchain paragraph, SECTION 19's provenance, the twin banner.
P-1 [parameter] The two_group_census docstring in SECTION 19 writes 4! / 2 = 14 beside a theorem that decides 12; 24 / 2 = 12. Falsifier: divide.
P-2 [provenance] SECTION 17 defines the formal reading as even under the execution flip and then calls the kinetic target "even too", two sentences after the same paragraph needs that target odd for the wall to hold at this seat; the source of the evenness label is the formal reading and it is applied to the wrong object. Falsifier: read the two sentences and the wall's hypotheses.
P-3 [parameter] SECTION 3's header claims the finite cardinal corollary derived for every finite frame from the general bijection; no theorem in the file states a cardinality, K4 decides 2^6 = 64 as arithmetic, and the bijection theorem's type carries no cardinal. Falsifier: search the file for a cardinality statement.
P-4 [provenance] The header's SESSION RECEIPTS block is labelled "(this build)" and reports the FAE/RAF engine boot executed at build time with its digest chain; the v1.1.4 pass executed no such boot, and the block is carried unchanged from v1.1.3. A receipt labelled as this build's that this build did not run is a provenance mislabel. Falsifier: the pass's own run logs, which contain no engine boot. FINDING.
P-5 [provenance] The pin: the lean-toolchain file names leanprover/lean4:v4.19.0 and the contract names the same; the 4.34.0 receipt is recorded as a run, not as a pass of gate three; the 150 differing guards are attributed to that toolchain's library and printer with the mechanism named. Recomputed from the run output. No finding.
P-6 [provenance] SECTION 19 carries its origin (the PSP codex's integrable part, cycle psp2) and its seven namespaces; its 48 declarations depend on no declared posit and on no choice principle, read off the roster. No finding.
P-7 [parameter] The twin banner says the twin's len_trim counts bytes and that no check moved; the twin's aegis_last_len is len_trim(logic_mode) and the battery reads 123 checks, 0 failures, identical to the v1.1.3 run log. No finding.
