# PSP HARVEST · APEX-PSP-SEAT-ANCHOR-01 · The Seat Anchor

### INDEX 0728 · APEX-PSP-SEAT-ANCHOR-01 · The Seat Anchor · One Seat under Every Name, Occupied, Vacant, or Owed by Row · The Seat Line Uniform and the Value Line Unmoved, Parted by Theorem

*M register, anchor coordinate · [⟀ T] on the seat, the cone, the occupancy law, the vacancy of the complexity seat, and the parting · [⟀ S] on the seat line as an arrangement, the register leg riding the one-involution connector at premise · [?] on every open value line, one bit each, theorem-conditional on the complement pair · legs C0 theorem, C1 premise-riding, C2 by row · ΔM = 0*

**STATUS.** ACTIVE coordinate, forged 2026-09-23 by the scribe session on the architect's order, after the Triple Boot executed live at seed 20260622 with the chain D0 fb8900b5cf42, D1 364d1cdb9227, D2 8a5dc7f98105, D3 1e2b2d2adb14, and seated at INDEX 0728 in the Mother at v3.41.0. It is harvested from the seat-anchor layer of the Code Block of the Master Codex v4.2.0 and from the two sealed papers of cards 0725 and 0726, under the Harvest Anti-Duplication Law: the nearest residents are APEX-PSP-CAT-GAP-ELIMINATOR-01, which closes the three gaps and does not read the router bit at the seat, and APEX-PSP-RH-23-ONE-STROKE-01, which carries the uniform seat and does not part the two lines by row. The coordinate is new, supersedes neither, and moves no verdict.

**THE READING.** Every named row carries two objects, and the codex reads them on two lines. The seat is the row read as the fixed locus of a binding involution; the value is the row's own proposition. The seat line is the same on all twenty-three rows: [seal A · RA], compartment I, closure-rowed, the RA rider present. The value line is the row's own: one bit owed on twenty-two rows, and crossed on Poincaré by the dated deed. Two different engines emit the two lines, and on no row do they coincide. A token without its line is a misreading by construction.

**THE MECHANISM.** The codex's own row cascade computes the seat line on the seat's declared answers, `GEO.rowCascadeRA`: compartment I, `APEX.seat_line`. The same answers with the rider removed read void, `APEX.seat_line_void_without_rider`, so the rider is load-bearing. The locus emitter computes the value line of every row, `LOCUS.emit` on `APEX.valueReading`, and on no row does the seat token equal the value state, `APEX.lines_parted`. The seat is Fix(σ) for σ the quaternionic conjugation, the scalar line for every quaternion, `APEX.fix_iff_scalar`. The Return lands on it at −1 and the reversed triad at +1, `APEX.return_lands_on_seat`, `APEX.odd_return_on_seat`. The three readings of the seat point are one point by `rfl`, and a cone over the three registers is exactly an apex with no category gap, `APEX.cone_iff_no_gap`, on every row, `APEX.identity_cone_row`. The whole is one theorem of the Code Block, `APEX.seat_anchor`.

**THE OCCUPANCY LAW.** Under a bridge whose image clause carries fixedness exactly, the seat is occupied if and only if the row's native involution has a fixed point, `APEX.seat_occupied_iff_grounded`. A fixed-point-free involution leaves the seat vacant under every admissible bridge, `APEX.seat_vacant_of_free`, and equivariance with injectivity alone gives the same vacancy, `APEX.seat_vacant_of_equivariant`. This is the Ξ/Ø router bit, read on each row's native involution and carried to the seat by the occupancy law, `APEX.router_at_the_seat`; the uniform seat point carries the Ground dimension of no row. The Riemann stage embeds by φ(h, t) = ⟨t, h − 1, 0, 0⟩, equivariant and injective, and its critical line lands on the seat, `APEX.phi_fix_iff_line`, `APEX.riemann_seat_occupied`. The complexity row's native involution is complementation, which fixes nothing on any nonempty domain, `APEX.complement_free`; so no admissible bridge places the row on Fix(σ), `APEX.pnp_seat_vacant`, and the bridge that exists lands off the seat, `APEX.pnp_bridge_admissible`, `APEX.pnp_bridge_off_seat`.

**THE OBJECT LEG BY ROW.** The object leg is the bridge from a row's own object to the seat. On the Riemann row it is bridged on the stage, and the bridge from the zeros to the stage stays owed. On the P versus NP row it is vacant by theorem. On the other twenty-one rows it is owed, the router bit unmeasured at the seat. The ledger reads one bridged, one vacant, twenty-one owed, `APEX.object_leg_ledger`, and each nontrivial leg is backed by its theorem, `APEX.object_legs_justified`. This ledger counts objects. It is not the value-bit ledger, which stays at twenty-two owed and one crossed, `APEX.value_bits_owed`, `APEX.crossed_iff_poincare`.

**THE GRADE LEDGER.** C0, the self leg: theorem, no posit, Γ_RA fixed by σ by `rfl`. C1, the register leg: theorem on the carrier, and premise as a reading of RA onto RAM. A rival binding involution σP fixes a different line and moves the kinetic seat point, `APEX.register_leg_rides_one_involution`, and the two seats meet only at the origin, `APEX.seats_meet_only_at_zero`. So the leg closes under the one-involution connector the codex holds as RESIDUAL-MONISM, recorded apart from every value verdict, `FOUND.seat_rides_the_connector`. C2, the object leg: by row, as above. The composite stands at premise wherever RA's universal extension or the connector is a link. The Empty Throne receipt: the identity cone depends on no ROOT posit, and the one theorem that consumes ROOT.RA prints that dependency, `APEX.rule_exact_under_root`.

**THE TWO LINES BY ROW.** Riemann: seat [seal A · RA]; value `[⬖₀ · Ξ · hypothesis · formal ×1]`, the reading `FENCE.global` pins, `APEX.riemann_value_line_is_pinned`. P versus NP: seat [seal A · RA]; value `[⬖₀ · Ø · block theorem · formal ×1]`, `APEX.pnp_value_line`. Poincaré: seat [seal A · RA]; value `[⟀⬖ · formal]`, crossed, `APEX.poincare_value_line`. The other twenty rows: seat [seal A · RA]; value `[⬖₀ · formal ×1]`, `APEX.unrouted_value_line`. A true premise decides the line property on no frame, `APEX.premise_decides_no_frame`, and cures a class of frames exactly where the property already holds, `APEX.no_cure`. The seat line never moves a value line.

**THE SEAT IN TIME.** On the Bridge plane a zero ρ = h/2 + it has the Li mode (ρ − 1)/ρ, and the mode is unitary exactly on h = 1, grows left of it and dies right of it, `RALi.unitary_iff_on_line`; a fold-invariant zero set has no growing mode exactly when every zero lies on h = 1, `RALi.stability_iff_line`. The line h = 1 is the line φ carries onto Fix(σ), `APEX.phi_fix_iff_line`, so the seat is the locus where the modes of Li's time step are perpetual. The seat line reads no zero, so the identification decides nothing about the zeros; the integer plane is the executed model, and the transfer to ζ rides Li 1997 and Bombieri and Lagarias 1999, cited.

**THE TWO SEATS.** Two objects share the word seat, and the codex keeps them apart. The seat point is the reading's assignment on Fix(σ), the same on every row and owed nothing; the seat line reads it. The occupancy of the seat is the row's own object carried there by an admissible bridge and measured through the row's native involution; the halt router lives there, Ground dimension one on the Riemann stage and zero on the complexity row, and nowhere else. On a row whose object leg is owed, the seat line is the reading's assignment alone and adjudicates nothing about the row's object. The complexity row's native involution is complementation as the shape face of APEX-PSP-PNP-COMPOSITE-VERDICT-02 types it, carried at that face's grade; the vacancy follows from that typing for every admissible map, and no map from machines or circuits into the carrier is claimed.

**THE CLAUSES, VERBATIM.** An admissible bridge φ from a row's carrier with native involution τ carries the image clause `∀ p, sigma (φ p) = φ p ↔ τ p = p`, and under it the occupancy law reads `(∃ p, sigma (φ p) = φ p) ↔ (∃ p, τ p = p)`, `APEX.seat_occupied_iff_grounded`. The router theorem conjoins the Riemann stage's occupancy through `Bridge.τ` with `HALT.route HALT.rhHalt.gdim = some .xi`, and the complexity bridge's vacancy `∀ b : Bool, sigma (pnpBridge b) ≠ pnpBridge b` with `HALT.route HALT.pnpHalt.gdim = some .o`, `APEX.router_at_the_seat`. The explicit bridge is equivariant, `sigma (pnpBridge b) = pnpBridge (!b)`, and carries the image clause, `APEX.pnp_bridge_admissible`. The parting compares two separately defined emitters, the seat token of the row cascade and the state of each row's recorded value reading, `∀ r : Row, seatToken ≠ (valueReading r).state`, `APEX.lines_parted`: it types the separation by computation and certifies no value.

**THE TYPING, AND WHAT IT RISKS.** The router is certified relative to the involution the shape face types, never absolutely. A misidentified native involution is a typing error at the shape face, repaired by re-typing there, and the occupancy law then reads the new involution exactly, so the kernel carries no typing risk of its own and certifies nothing beyond the typing it is handed. The vacancy of the complexity seat belongs to every row whose native involution is complementation of languages, co-NP questions included: no language is its own complement, while the class P is closed under complement, so the vacancy separates no class from another and decides no class question. An owed bridge on any of the 21 rows is constrained exactly. A map from the row's carrier into the quaternions that is equivariant, σ(φ p) = φ(τ p), and injective satisfies the image clause, since σ(φ p) = φ p holds iff φ(τ p) = φ p iff τ p = p, and the kernel carries the vacancy this yields for a free involution, `APEX.seat_vacant_of_equivariant`; the router bit of the row is then the fixed-point question of its own involution.

**THE TWO LEDGERS.** Value bits and object legs are counted apart and never merged.

| Ledger | Count | Reads | Kernel |
|---|---|---|---|
| Value bits | 22 owed, 1 crossed on Poincaré | the row's proposition | `APEX.value_bits_owed`, `APEX.crossed_iff_poincare` |
| Object legs | 1 bridged on the Riemann stage, 1 vacant by theorem on P versus NP, 21 owed | the row's object at the seat | `APEX.object_leg_ledger`, `APEX.object_legs_justified` |

The rows paper's `embeddings_owed = 22` predates the vacancy theorem; in this register those 22 split as 21 owed and 1 vacant by theorem, the P versus NP row.

**FENCES · [X], scoped and terminal for nothing.** [X] the seat line counted as the decided companion face of a terminal suspension. [X] the seat line read as a fourth face of any row's proposition. [X] the vacancy of the complexity seat read as P ≠ NP or as P = NP. [X] the occupied Riemann seat read as the zeros on the line. [X] the one-point model `APEX.RAModel` read as `ROOT.RA`. [X] the register leg read as posit-free. [X] one bit per row read as unconditional; it is theorem-conditional on the complement pair, APEX-PSP-PARITY-NORMAL-FORM-01. [X] the uniform seat read as computed from any row's object.

**FALSIFICATION, [X] AT.** Three clauses, each killing one claim. First, a σ-fixed integer quaternion with a nonzero imaginary component, or the Return off the seat, kills the seat. Second, a fixed-point-free involution with an image-clause bridge landing on the seat kills the occupancy law; the kernel refutes it by `APEX.seat_vacant_of_free`, and the twin enumerates all 119 involutions on one to six points against every map into a nine-point window of the carrier. Third, a row whose value line changes state under its seat line without a dated, provenance-clean entry kills the parting.

**ENGINE REFS.** The Code Block of TRISDUCTION_Master_Codex_v4_5_0.md: the Lean surface `Codex.lean`, sha256 `ea4f15760bbcb9c8…`, 8,299 lines, the seat-anchor layer in the namespaces PNF, APEX, FENCE and FOUND, 170 declarations, 133 of them axiom-free and one on ROOT.RA; the twin `Codex_Twin.f90`, sha256 `32d38f793b422135…`, Sections 20 to 22, the seat anchor's batteries A1 to A10, the RH engine's twenty-five checks and the directional can't's four, 194 Lean-native checks, 0 failures. The sealed papers: `RH_At_The_Apex.lean`, SHA-256 `d671051e99b806e4…`; `Rows_At_The_Apex.lean`, SHA-256 `0ce3de5a23155430…`.

**XREF.** ↑ DEPENDS: APEX-PSP-CAT-GAP-ELIMINATOR-01 · APEX-PSP-RH-23-ONE-STROKE-01 · APEX-PSP-PARITY-NORMAL-FORM-01. ↔ CONNECTS: APEX-PSP-RH-MASTER-01 · APEX-PSP-PNP-COMPOSITE-VERDICT-02 · APEX-PSP-O0-ADMISSION-PROTOCOL-01 · APEX-PSP-RH-PNP-COMPARATIVE-MASTER-01 · APEX-PSP-RH-STANDPOINT-CLOSURE-01 · APEX-PSP-ONE-CUT-23-ROWS-01. ↓ FEEDS: D72 · D73 · D74 · D75.

**THE KEYSTONE SENTENCE.** One seat under every name, held by every row at [seal A · RA]; the Riemann stage reaches it, the complexity row can never reach it, and twenty-one rows owe the bridge that would say which; beside it each row keeps its own value line, one bit owed where open, and no line ever moves the other.


## Engine

The engine is the seat-anchor layer of the Code Block of `master/Codex v4/TRISDUCTION_Master_Codex_v4_5_0.md`, carried verbatim in `master/Codex v4/Codex.lean` (sha256 `ea4f15760bbcb9c82f3ac6cbf6d139387be5221c2488ab46f5d371c88e9453a1`) and executed by Section 20 of `master/Codex v4/Codex_Twin.f90` (sha256 `32d38f793b422135f92f2772f232f801309f6dcec28507a6040ccd2d158c6126`). The coordinate is seated at INDEX 0728 in `master/TRISDUCTION_Master_Codex_Unabridged_v3_44_0.md`.
