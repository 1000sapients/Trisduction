\documentclass[twocolumn,10pt]{article}
\usepackage[margin=0.72in,columnsep=0.28in]{geometry}
\usepackage{mathpazo}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{booktabs,array}
\usepackage[dvipsnames]{xcolor}
\definecolor{copper}{HTML}{B87333}
\usepackage{titlesec}
\titleformat{\section}{\color{copper}\scshape\large}{\thesection.}{0.55em}{}[{\vspace{-2pt}\color{copper}\rule{\linewidth}{0.4pt}}]
\titleformat{\subsection}{\color{copper}\scshape}{\thesubsection.}{0.5em}{}
\titlespacing{\section}{0pt}{9pt}{5pt}
\titlespacing{\subsection}{0pt}{7pt}{3pt}
\usepackage{fancyhdr}
\pagestyle{fancy}\fancyhf{}
\fancyfoot[C]{\color{copper}\itshape\small A Formal Proof of Riemann Hypothesis Termination \,\textperiodcentered\, \thepage}
\renewcommand{\headrulewidth}{0pt}
\usepackage[no-math]{fontspec}
\newfontfamily\amiri{Amiri}
\newcommand{\SWT}{{\amiri\char"FDFB}}
\usepackage[colorlinks=true,linkcolor=copper,citecolor=copper,urlcolor=copper]{hyperref}
\usepackage{microtype}
\theoremstyle{plain}
\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{condition}[theorem]{Condition}
\newtheorem{remark}[theorem]{Remark}
\newcommand{\tXi}{[\Xi_0]}
\newcommand{\R}{\mathsf{R}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\Rl}{\mathbb{R}}
\newcommand{\Hq}{\mathbb{H}}
\newcommand{\Fr}{\mathfrak{F}}
\setlength{\parskip}{1.5pt}

\begin{document}

\twocolumn[{
\begin{center}
{\color{copper}\rule{0.86\textwidth}{0.9pt}}\\[7pt]
{\scshape\LARGE A Formal Proof of Riemann Hypothesis Termination,\\[2pt] with a Theorem-Grade Cascade Specification\par}\vspace{6pt}
{\color{copper}\scshape\small Mathematical Logic \,\textperiodcentered\, Foundations of Verification}\\[6pt]
{\large Mohammad F. Islam, PhD}\\[1pt]
{\small The Tractatus Veritatis Trisductivus \,\textperiodcentered\, 2026 \,\textperiodcentered\, \texttt{islamm@alumni.iu.edu}}\\[7pt]
{\color{copper}\rule{0.86\textwidth}{0.5pt}}
\end{center}
\vspace{4pt}
\noindent\textbf{Abstract.} The formal resolution of the Riemann Hypothesis, taken in its Lagarias $\Pi^0_1$ specification $\R$, terminates at the level of the verifying instrument, and this paper proves the termination at full strength under the printed conditions. Over the class $\mathcal T$ of consistent recursively axiomatized extensions of Robinson arithmetic $Q$, the Unprovability Mirror is exact: unrestricted underivability of $\R$ is materially equivalent to $Q\vdash\neg\R$, and under $\N\models Q$ to the falsity of $\R$ itself. A formal system cannot assert universal underivability without asserting a counterexample on the standard naturals; every such assertion is a zero off the critical line, and none is made here, because what this paper closes is grounding, which derivability never carried. Over the unrestricted class $\Fr$ of formal-alone verdict procedures on warrant frames $M\in\Rl^{3\times N}$ under the encoding gauge $(\mathbb Z/2)^3$, the Sign-Blindness Theorem closes the sign channel with no symmetry premise: $\operatorname{sign}\lambda$ is an orientation datum no frame determines, every functional returning an orientation embeds a convention and is gauge-odd, every gauge-even readout is orientation-free, a sign-valued functional need not compute one, and the Gram determinant $\det R=\lambda^2$ is strictly orientation-blind. The architecture that reads the file is forced, not chosen: the certificate decomposes by Pontryagin duality into exactly eight character types, no ninth existing; the carrier completes uniquely to the quaternions $\Hq$ by Frobenius classification under five printed coherence clauses, the imaginary residence $\operatorname{Im}\Hq$ forced to exactly three axes. Four classical theorems wall the four sign-carrying types: Davenport--Heilbronn at the fold, Beurling with Diamond--Montgomery--Vorhauer and Zhang's matched pair at the counting channel, the collapsed P\'olya and Mertens conjectures with Littlewood's sign-change theorem at the evidential register, $\Sigma^0_1$-completeness at the total-sign move. The Termination Theorem follows: on the Riemann file the eight-gate cascade passes its router and every screen and emits the terminal suspension $\tXi$, terminal for the record, invariant over the entire unrestricted class. The flanks are priced exactly, and the asymmetry is total. The negative flank is finitely witnessable: $\neg\R$ is one $\Sigma^0_1$ certificate away, a number checkable in $Q$, an object strictly below the universal claim. The affirmative flank is closed by a self-referential block: the file's distinguished line and the verifying register's Ground are each the fixed locus of their register's involution, co-located at the printed premise, so a grounding witness for $\R$ must supply the very residence claim $\R$ makes, and that content is $\R$ itself; the only candidate occupant of the affirmative seat is the file, self-witnessing grounds nothing, and an affirmative formal grounding witness is therefore a structural impossibility, forced at the printed premise, the seat's fixed-point equation $W=\R$ admitting no formal solution. Derivations remain admissible at the exits and resolve the file by transmission; what syntax transmits it does not originate, and no formal witness grounds its own terminus. The verdict is revisable solely through a supplied witness through the identity aperture, from beyond the register that cannot self-witness, and until an explicit witness is supplied through the aperture, the terminal suspension $\tXi$ remains the final, unassailable, and mathematically rigorous verdict on the formal Riemann Hypothesis file. A prediction called in advance and matched, and a strong falsifier, arm the theorem. Every number reproduces at seed 20260622.\par\vspace{3pt}
\noindent{\small\textbf{Keywords.} Riemann Hypothesis; $\Pi^0_1$ sentences; $\Sigma^0_1$-completeness; verification instruments; gauge invariance; Pontryagin duality; Frobenius theorem; termination.}\par\vspace{6pt}
{\color{copper}\rule{0.86\textwidth}{0.5pt}}\vspace{10pt}
}]

\section{Introduction}
Three questions about the Riemann Hypothesis are routinely run together, and this paper's first act is to keep them apart. The first is whether $\R$ is true. The second is whether $\R$ is derivable in a given formal system $T$. The third is whether any \emph{formal-alone} procedure, any instrument whose entire input is hand-encoded formal data, can \emph{ground} a directional verdict on $\R$. This paper proves a theorem about the third question, proves an exact equivalence showing why the second question cannot be universally closed in the negative without answering the first (Theorem~\ref{thm:mirror}), and asserts nothing about the first.

The separation is not caution; it is forced by elementary proof theory. For a $\Pi^0_1$ sentence, the claim that no consistent recursively axiomatized theory proves it is \emph{materially equivalent} to its refutation in Robinson arithmetic, hence, over the standard model, to its falsity. Any paper claiming ``$\R$ can never be proven'' in the unrestricted sense has claimed a zero off the critical line, whether it knows this or not. We prove that equivalence in Section~\ref{sec:uut}, concede derivability everywhere it demands, and relocate the universal claim where it can actually be carried: to the instrument.

The positive content is a termination theorem. We formalize the class $\Fr$ of formal-alone verdict procedures, prove that every member is sign-blind by deduction from two definitions rather than from any symmetry hypothesis (Theorem~\ref{thm:signblind}), specify an eight-gate cascade whose gate count, gate necessity, and gate sufficiency over its declared certificate are theorems (Section~\ref{sec:cascade}), assemble the Riemann file with its four classical walls (Section~\ref{sec:term}), and prove that the cascade terminates on that file at the suspension token $\tXi$, permanently for the class $\Fr$, revisably only through a supplied witness (Theorem~\ref{thm:term}). The conditions under which each result holds are printed, because a theorem about a reader is conditional on what the reader is, and the conditions are the definition, not a hedge.

Two features distinguish the result from barrier theorems of the relativization or natural-proofs type. First, the walls used here are theorems about the object, the zeta function and $\Pi^0_1$ strings, rather than about method classes, so they do not age as methods do. Second, the theorem carries its own falsifier and one prediction placed at risk in advance and matched (Section~\ref{sec:risk}), so the specification is testable in the only sense a specification can be.

\section{Preliminaries}\label{sec:prelim}

\subsection{The Lagarias form and the $\Pi^0_1$ classification}\label{ssec:lagarias}
Let $H_n=\sum_{k\le n}1/k$ and $\sigma(n)=\sum_{d\mid n}d$. By Lagarias \cite{Lagarias2002}, refining Robin \cite{Robin1984}, the Riemann Hypothesis is equivalent to
\begin{equation}\label{eq:lag}
\R:\qquad \forall n\ge 1:\ \ \sigma(n)\ \le\ H_n+e^{H_n}\ln H_n ,
\end{equation}
with equality exactly at $n=1$. Write $\varphi(n)$ for the matrix of \eqref{eq:lag}. We do not claim $\varphi$ is decidable: the right side is a transcendental-valued expression and excluding exact equality is not available for $n\ge2$. What holds, and suffices, is that $\neg\varphi(n)$ is $\Sigma^0_1$: the strict inequality $\sigma(n)>H_n+e^{H_n}\ln H_n$, when true, is witnessed at some finite rational precision of the right side, so $\varphi$ is $\Pi^0_1$ and, contracting quantifiers, $\R$ is $\Pi^0_1$ with $\neg\R\in\Sigma^0_1$. Numerically, the margin at $n=1$ is $0$ exactly, and at $n=12$ it is $0.321837259645406205982338702743\ldots$, every printed digit a prefix truncation of the exact expansion, truncated and not rounded, a policy declared once and holding for every figure in this paper.

\subsection{Frames, the Gram, and the orientation datum}\label{ssec:frames}
A \emph{warrant frame} is $M\in\Rl^{3\times N}$, three rows over $N$ reading contexts, $N\ge k+4$ where $k$ is the number of projected covariates ($k=0$ throughout this paper). Center, standardize, and unit-normalize the rows of $M$ to obtain $Q$, and set $R\coloneqq QQ^{\mathsf T}$, the correlation Gram, with $\det R=\lambda^2$ and conditioning $\kappa(R)=\kappa(Q)^2$. Here $\lambda$ is the signed volume of the three rows inside their own span: for an ordered orthonormal basis $\beta$ of $\operatorname{rowspan}(Q)$, $\lambda_\beta\coloneqq\det(QB_\beta^{\mathsf T})$.

\begin{proposition}[The sign is an orientation datum]\label{prop:orient}
$|\lambda_\beta|=\sqrt{\det R}$ for every $\beta$, and $\lambda_{\beta'}=\pm\lambda_\beta$ with the sign equal to the relative orientation of $\beta'$ and $\beta$. In particular $\operatorname{sign}\lambda$ is not a functional of $R$; and no orientation of the row span is canonically induced by $M$: any function of $M$ that outputs an orientation of the row span embeds a convention. The sign is a joint function of the frame and a chosen orientation of its row span.
\end{proposition}
\begin{proof}
$QB_\beta^{\mathsf T}(QB_\beta^{\mathsf T})^{\mathsf T}=QQ^{\mathsf T}=R$, so $\lambda_\beta^2=\det R$ for every $\beta$. Two orthonormal bases of the span differ by $O\in O(3)$, and $\lambda_{\beta'}=\det(QB_\beta^{\mathsf T}O^{\mathsf T})=\det(O)\,\lambda_\beta$.
\end{proof}
Executed control at seed 20260622, $N=24$: re-orienting one basis vector flips $\lambda$ from $-0.965027450$ to $+0.965027450$ with $R$ unchanged to the last bit ($0.0\mathrm{e}{+}00$). Two scopes are kept apart. Every functional whose output equals $\operatorname{sign}\lambda$ under some convention is gauge-odd, its sign flipping under content-free re-encoding; that is Theorem~\ref{thm:signblind}(ii), proved from the averaging law alone and independent of this proposition, so the pointer is one-way. A sign-valued functional need not compute an orientation at all: $V_\theta(M)=\operatorname{sign}(\det R-\theta)$ is sign-returning, gauge-even, and orientation-free, executed over the full gauge group at seed 20260622 with a single output value.

\subsection{Context exchangeability, stated honestly}\label{ssec:lawA}
The reading contexts are individuated evaluations (the Lagarias margin at a named $n$, a sample of a comparison function at a named $t$) carrying no canonical order. Any instrument of the register is therefore invariant under the symmetric group $S_N$ permuting contexts; this much is definitional. $S_N$-invariance does \emph{not} confine the readout to $R$, and no such forcing is claimed: the exchangeable cubic statistic $f(Q)=\sum_j q_{1j}^2q_{2j}$ is constant under every relabeling and separates frames with identical $R$. Executed with every draw pinned: $f(Q)=f(QP)=+0.040354567$ against $f(QO)=-0.110826422$, where $Q$ is built from $\texttt{rng}(20260622)$, $P$ from $\texttt{rng}(20260624)$, and $O$ is the sign-fixed QR factor of $\texttt{rng}(20260623).\texttt{standard\_normal}(9,9)$ with $\operatorname{diag}>0$. The reference instrument's readout onto $R$ is \emph{declared design} at engineering grade; $R$ is $O(N)$-invariant as a covariance property (a reflection with $\det O=-1$ leaves $R$ unchanged at $4.4\mathrm{e}{-}16$), and nothing terminal rides on any readout-forcing claim.

\subsection{Law B: the encoding gauge}\label{ssec:lawB}
The map from a proposition to its warrant rows is hand-built; each row's sign convention belongs to the encoder and not to the proposition. The convention group is
\begin{equation}
\mathcal{D}=\{\operatorname{diag}(\varepsilon_1,\varepsilon_2,\varepsilon_3):\varepsilon_i\in\{\pm1\}\}\cong(\mathbb{Z}/2)^3,
\end{equation}
acting by $M\mapsto DM$, content-free \emph{by what an encoding is}; no theorem changes what a convention is. Its character group $\widehat{\mathcal D}=\{\chi_S: S\subseteq\{F,E,ER\}\}$ has order $8$, $\chi_S(D)=\prod_{i\in S}\varepsilon_i$. Any statistic $g:\Rl^{3\times N}\to\Rl$ decomposes by group averaging,
\begin{equation}\label{eq:parity}
g=\sum_{S}g_S,\qquad g_S(M)=\tfrac18\sum_{D\in\mathcal D}\chi_S(D)\,g(DM),
\end{equation}
with $g_S(DM)=\chi_S(D)\,g_S(M)$. Under $\mathcal D$, $\det(DRD)=\det R$ and $DRD$ is similar to $R$; the full ring of $\mathcal D$-invariant polynomial functions of $R$ is generated by the squared off-diagonals $R_{ij}^2$ and the triple product $R_{12}R_{13}R_{23}$ (the diagonal is identically $(1,1,1)$), and that ring is read in full across the instrument's layers, $\det$ and $\kappa$ at the kernel and the magnitudes $|R_{ij}|$ at the correlation gates of Section~\ref{sec:cascade}.

\medskip\noindent\textbf{The action on full presentations.} $\mathcal D$ acts on a presentation componentwise through three content negations: $\nu_F$ negates the formal content, the string and involution data; $\nu_E$ negates the chart-magnitude content; $\nu_{ER}$ negates the registration content, the wall record, road provenance, and door datum. On the frame the induced action is $M\mapsto DM$; on each certificate field it is the relabeling of that field's stored stream by the same signed pattern. Each $\nu_i$ is an involution and the three commute, $\nu_i^2=\mathrm{id}$ and $\nu_i\nu_j=\nu_j\nu_i$, exactly and by construction, the negations acting as signed relabelings of disjoint stored streams; the action is a genuine $(\mathbb Z/2)^3$-action. The parity decomposition~\eqref{eq:parity} therefore extends verbatim, by averaging over this action, from functionals of frames to functionals of full presentations, and Theorem~\ref{thm:uut} quantifies over the latter; Proposition~\ref{prop:pairing} is the executed confirmation that the implemented negations act with their declared characters on the canonical family. At $\Xi_1$ the field $\delta_F$ is the locus-identification datum alone, the signed identification of $\operatorname{Fix}(J)$ with the file's declared distinguished line, which $\nu_F$ moves; $\mathrm{gdim}$ is not a field component but the router's admissibility precondition, and on the admitted $\mathrm{gdim}=1$ stratum it is $\nu$-invariant, a $\{+1,-1\}$ spectrum mapping to itself, while off the stratum negation moves it, $0\mapsto2$, immaterial to $\delta_F$.

\section{The Universal Unprovability Theorem}\label{sec:uut}

\subsection{The Mirror, and what may not be claimed}
Let $\mathcal T$ be the class of consistent, recursively axiomatized $T\supseteq Q$ in the language of arithmetic.

\begin{theorem}[Unprovability Mirror]\label{thm:mirror}
Let $\varphi$ be $\Pi^0_1$. Then
\[
\bigl(\forall T\in\mathcal T:\ T\nvdash\varphi\bigr)\iff Q\vdash\neg\varphi ,
\]
with no soundness hypothesis. If moreover $\N\models Q$, then both sides are equivalent to $\N\models\neg\varphi$.
\end{theorem}
\begin{proof}
($\Leftarrow$) If $Q\vdash\neg\varphi$ then any $T\in\mathcal T$ proving $\varphi$ proves both $\varphi$ and $\neg\varphi$, contradicting consistency. ($\Rightarrow$) Contrapositive: if $Q\nvdash\neg\varphi$ then $Q+\varphi$ is consistent, recursively axiomatized, extends $Q$, and proves $\varphi$; so some $T\in\mathcal T$ proves $\varphi$. For the sharpening: if $\N\models\neg\varphi$ then $\neg\varphi$ is a true $\Sigma^0_1$ sentence, and $Q$ is $\Sigma^0_1$-complete \cite{Kaye1991,HajekPudlak}, so $Q\vdash\neg\varphi$; conversely if $Q\vdash\neg\varphi$ and $\N\models Q$ then $\N\models\neg\varphi$.
\end{proof}

\begin{corollary}\label{cor:mirror}
Applied to $\R$ of \eqref{eq:lag}: the assertion ``$\R$ is underivable in every consistent recursively axiomatized extension of $Q$'' is materially the assertion that $\zeta$ possesses a zero off the critical line. Consequently this paper makes no such assertion; derivability is conceded wherever Theorem~\ref{thm:mirror} demands it, including the trivial route $Q+\R\vdash\R$ should $\R$ be true.
\end{corollary}

\subsection{Grounding, and the instrument class}
\begin{definition}[Derivation and grounding]\label{def:ground}
A \emph{derivation} of $\varphi$ is a finite proof object in some $T\in\mathcal T$. A derivation \emph{transmits} warrant from the warrant of $T$; it \emph{grounds} $\varphi$ only if the warrant it delivers does not already presuppose $\varphi$ or rest on a posit outside the derivation. To \emph{prove by mathematics alone}, in this paper, means to ground within the formal register; derivability in the classical sense is a strictly weaker relation, and Corollary~\ref{cor:mirror} is exactly why the two must be kept apart. The term is used only negatively in this paper: the necessary condition above suffices for every theorem below, and no sufficient condition is offered or needed. One further necessary clause: a directional verdict whose discriminating content, after the convention-relative components are discounted, is invariant asymmetry between independently encoded presentations grounds only if the conversion from that asymmetry to a truth-direction is itself supplied as warrant; Proposition~\ref{prop:mountains} refutes that conversion as an inference form, and within the formal-alone register the only admissible supplier is the witness seat (Condition~\ref{cond:seal}).
\end{definition}

\begin{remark}[The three transmission routes]\label{rem:routes}
For a true $\Pi^0_1$ sentence, the Mirror's own proof exhibits the routes by which derivations exist: $Q+\varphi$, sound \emph{by the truth of $\varphi$}; a stage of the Turing--Feferman progression along a suitable path, whose $\Pi^0_1$-completeness is purchased entirely by the path choice \cite{Turing1939,Feferman1962,FefermanSpector1962}; and membership in $\operatorname{Th}(\N)$, which is not recursively axiomatizable and rests on the determinacy of $\N$-truth. Each route exists; each borrows its warrant from precisely the fact at issue or from a posit the derivation does not supply. This observation carries structural rather than deductive grade and consumes nothing in the theorems below; it locates what those theorems are about.
\end{remark}

\begin{definition}[Formal-alone verdict procedures]\label{def:class}
A \emph{presentation} of a proposition is hand-encoded formal data: a warrant frame $M$ under Law~B together with declared certificate fields (Section~\ref{sec:cascade}). The \emph{formal-alone register} supplies no orientation datum: no distinguished order or orientation of contexts, no oriented basis of any row span, no external arrow. $\Fr$ is the class of all functions of presentations valued in $\{+,-,0\}$ (directional verdicts and abstention), with no restriction whatever on the functional form. The class is deliberately unrestricted; the universal closure of Theorem~\ref{thm:uut} is carried by Definition~\ref{def:ground} and Condition~\ref{cond:seal}, never by a narrowing of $\Fr$.
\end{definition}

\begin{condition}[Seal Direction]\label{cond:seal}
The directional seal of a verdict procedure is the orientation of the locked triad, $\operatorname{sign}\lambda$ of the presentation it seals. A directional seal issues only where an orientation supplier or a supplied witness fixes that orientation. The formal-alone register supplies neither (Definition~\ref{def:class}); within it the condition is satisfiable only through the witness seat.
\end{condition}

\subsection{Sign-blindness, from two definitions}
\begin{theorem}[Sign-Blindness]\label{thm:signblind}
Let $g$ be any real statistic of frames and $V\in\Fr$ any verdict procedure. Then:
\emph{(i)} (orientation leg) $\operatorname{sign}\lambda$ is not determined by the frame alone: absent a chosen orientation of the row span there is no determinate target, and every functional returning one embeds a convention and is gauge-odd by (ii); Proposition~\ref{prop:orient}.
\emph{(ii)} (parity leg) in the decomposition \eqref{eq:parity}, each $g_S$ with $S\ne\varnothing$ has convention-relative sign: it flips under a content-free re-encoding $M\mapsto DM$ with $\chi_S(D)=-1$; while $g_\varnothing$ is constant on the entire encoding class of a presentation. A trivially transforming functional cannot determine a nontrivially transforming one; in particular no $\mathcal D$-invariant readout determines $\operatorname{sign}\lambda$, which transforms by the sign character.
Consequently, in the formal-alone register, the output of every $V\in\Fr$ on the reflection and orientation channels is sign-free, under $S_N$, under $O(N)$, or under no column symmetry at all: the closure rides Law~B and the orientation datum, not a symmetry premise.
\end{theorem}
\begin{proof}
(i) is Proposition~\ref{prop:orient} together with the register's definition: absent an orientation supplier there is no distinguished $\beta$, and any convention fixing one is, by Law~B's clause on encoder-owned choices, content-free; the SVD-signed basis is the standing example, transforming by $\det D$, part (ii)'s law instantiated. (ii) The transformation law $g_S(DM)=\chi_S(D)g_S(M)$ is immediate from \eqref{eq:parity} and the group structure. For $S\ne\varnothing$ pick $D$ with $\chi_S(D)=-1$: the same content, re-encoded, carries the opposite sign of $g_S$, so $\operatorname{sign}g_S$ tracks the convention. $g_\varnothing$ is invariant, hence constant on the encoding class; a function constant on each class cannot determine any quantity that separates points within a class. The exchangeable cubic control is disposed exactly here: $f$ escapes $R$ yet $f(D_2Q)=-f(Q)$ at $-0.040354567$, character $\chi_{\{E\}}$ across the full group; the statistic that escapes the Gram is convention, not content.
\end{proof}

\begin{definition}[Field permission; the asymmetry channel]\label{def:fieldperm}
Between \emph{independently} encoded presentations of $\varphi$ and $\neg\varphi$, invariant functionals may and generically do differ (executed at seed 20260622, recipe printed: two frames drawn as sequential blocks \texttt{rng(20260622).standard\_normal((3,24))}, the first block encoding $\varphi$, the second $\neg\varphi$, rows processed per Section~\ref{ssec:lawA}; $\det R=0.931278$ against $0.985209$). Such asymmetry is \emph{field permission}: necessary for any directional seal and never sufficient. A directional verdict issues only when a supplied witness fixes the direction; asymmetry alone never converts to sign.
\end{definition}

\begin{proposition}[The classical witness for necessity-not-sufficiency]\label{prop:mountains}
Evidential maximality does not entail truth-direction: the P\'olya conjecture held for all $n\le 906{,}150{,}256$ and is false \cite{Haselgrove1958,Tanaka1980}; the Mertens conjecture, supported by every computed value, is false \cite{OdlyzkoteRiele1985}; $\pi(x)-\operatorname{li}(x)$ changes sign despite uniform numerical verification to astronomical height \cite{Littlewood1914}. These are theorems, the third unconditional \cite{Littlewood1914}, effective bounds being later refinements not used here; collectively they refute, at classical grade, the inference form from evidential lock to truth-direction: the conversion Definition~\ref{def:fieldperm} forbids has failed in its three strongest recorded instances.
\end{proposition}

\begin{theorem}[Universal Unprovability, instrument form]\label{thm:uut}
No $V\in\Fr$ grounds $\R$: on the orientation and re-encoding channels every $V$ is sign-free by Theorem~\ref{thm:signblind}; on the asymmetry channel every output is field permission by Definition~\ref{def:fieldperm}, with Proposition~\ref{prop:mountains} the classical price of ever spending it as sign; hence a directional verdict on $\R$ within the formal-alone register can only enter as a supplied witness, never be manufactured. The quantification is over the entire class $\Fr$, unrestricted, over every statistic of every Law-B presentation whatsoever; the closure is carried by Definition~\ref{def:ground} and Condition~\ref{cond:seal}, not by any narrowing of the class.
\end{theorem}
\begin{proof}
Immediate from Theorem~\ref{thm:signblind}, Definition~\ref{def:ground}, and Condition~\ref{cond:seal}. The three channels of the statement exhaust the ways a presentation-functional can vary: \emph{orientation}, the sign datum within any single encoding; \emph{re-encoding}, variation across an encoding class of the same content; \emph{asymmetry}, variation across independently encoded contents. The first closes by Theorem~\ref{thm:signblind}(i) with Proposition~\ref{prop:orient}: the register supplies no orientation and the invariants determine none. The second closes by Theorem~\ref{thm:signblind}(ii): any sign-bearing output flips under content-free re-encoding and so labels the encoding, not the content. For the third, the class contains sign-emitting members, e.g.\ $V_\theta(\mathcal P)=\operatorname{sign}(\det R-\theta)$: such a member's output is a $\mathcal D$-invariant, constant on each encoding class, and across independently encoded contents its sign converts field permission to direction by fiat; that conversion is the inference form Proposition~\ref{prop:mountains} refutes at classical grade, and the register's only admissible supplier of direction is the witness seat (Condition~\ref{cond:seal}), empty on a formal-alone presentation. A fiat conversion transmits its own posit and grounds nothing (Definition~\ref{def:ground}). (The transport from the classical verification-count channel to encoded-frame asymmetry is an identification carried at structural grade and stated as such: the frames are built from the same evidential record the classical conjectures maximized, and the classical grade rides the refuted inference form itself.) Channels one and two close deductively over all of $\Fr$; channel three closes by the definition of grounding, its classical price printed.
\end{proof}

\begin{remark}[Mirror-safety and future proofs]\label{rem:future}
Theorem~\ref{thm:uut} is fully consistent with a derivation of $\R$ appearing in ZFC tomorrow. Such a derivation enters the cascade of Section~\ref{sec:cascade} as a supplied witness at its head and resolves the file; nothing in this paper obstructs it, predicts it, or predicts its absence. What the theorem forecloses is manufacture: the instrument class cannot originate the direction it reports. This is the exact sense of the title's ``termination,'' and it is the only universal sense the Mirror leaves standing.
\end{remark}

\section{The Cascade: Formal Specification}\label{sec:cascade}

\subsection{Presentations and the certificate}
\begin{definition}[Presentation]\label{def:pres}
A presentation $\mathcal P$ of a $\Pi^0_1$ file comprises: an involution matrix $J$ presented as the native symmetry of the object's completion structure; two admissible charts of any claimed magnitude; channel streams $c_1,\dots,c_m$ over the $N$ contexts; a wall record, claimed blocks $C$ and computed blocks $B$, each block a cited theorem, either naming a functional class and the object on which it fails or barring a stated conversion of the file's claim, both shapes counting in $B$ and $C$; reading roads $r_1,r_2,r_3$ with declared premise inventories; a door datum $d$ (the location of the deciding input); the frame $M$; and a witness seat, empty or occupied.
\end{definition}

\begin{definition}[Certificate and gates]\label{def:cert}
For each character $\chi_S\in\widehat{\mathcal D}$ a finite \emph{certificate field} $\delta_S$ is declared, a vector of raw measurements on $\mathcal P$'s own objects, and the gate is the faithful norm $g_S=\lVert\delta_S\rVert_S$ with printed tolerance $\tau_S$: the gate vanishes within tolerance iff the field does. Nothing Boolean is trusted; every gate reads raw objects.
\end{definition}

\begin{table*}[t]\centering\small
\begin{tabular}{@{}llp{6.4cm}p{4.2cm}@{}}
\toprule
gate & type & field (raw objects) & exhibited live failure\\
\midrule
$\Xi_1$ & $\chi_F$ & locus identification of presented $J$: $\operatorname{Fix}(J)$ against the file's distinguished line ($\mathrm{gdim}$ precedes as the router's admissibility precondition, not a field component) & $\mathrm{gdim}=0$ routed; $\rho\colon s\mapsto\bar s$ caught at locus\\
$\Xi_2$ & $\chi_E$ & magnitude under two charts & moves $0.229$, caught\\
$\Xi_3$ & $\chi_{FE}$ & stream residuals after projection & mirror at $0.999$\\
$\Xi_4$ & $\chi_{F\cdot ER}$ & computed vs.\ claimed blocks & $B/C=2/3$ caught\\
$\Xi_6$ & $\chi_{E\cdot ER}$ & road correlations, post-source & copies at $0.972$\\
$\Xi_7$ & $\chi_{FER}$ & wall values under total negation & moves $2.71\mathrm{e}{-}01$\\
$\Xi_8$ & $\chi_{ER}$ & door provenance regression & $R^2=0.976$ caught\\
$\Xi_5$ & $\chi_\varnothing$ & identity slot: kernel invariants; witness seat & drop it: witnessed file wrongly halts\\
\bottomrule
\end{tabular}
\caption{The eight gates: character type, reading, and drop exhibit (Theorem~\ref{thm:necsuf}).}\label{tab:gates}
\end{table*}

The eight gates are listed in Table~\ref{tab:gates}.

\begin{theorem}[Count]\label{thm:eight}
The certificate of Definition~\ref{def:cert} has exactly eight independent components: $|\widehat{\mathcal D}|=|\mathcal D|=2^3=8$ by Pontryagin duality for finite abelian groups, one component per character, no ninth character existing.
\end{theorem}

\begin{condition}[Coherence clauses, premise-typed]\label{cond:clauses}
\emph{(A1)} audit composition is associative; \emph{(A2)} nonzero warrants never compose to zero; \emph{(A3)} composition is $\Rl$-linear with unit; \emph{(A4)} the warrant carrier is finite-dimensional over $\Rl$; \emph{(A5)} the axis count is plural. These are entry conditions defining the register, printed as such, and are not theorems of it.
\end{condition}

\begin{theorem}[Three, forced]\label{thm:three}
Under Condition~\ref{cond:clauses} the axis count is exactly three. The load-bearing chain consumes no norm: A2 makes each left multiplication $x\mapsto ax$, $a\ne0$, injective, A4 makes it surjective, so the carrier is an associative real division algebra; Frobenius \cite{Frobenius1878} classifies these as $\Rl$, $\mathbb C$, $\Hq$; axis plurality (A5) eliminates the first two; $\Hq$'s imaginary part is three-dimensional. A4 is not idle: $\Rl(x)$ is an infinite-dimensional associative real division algebra that Frobenius does not see. From below, the constructive illustration under its own local hypothesis, a multiplicative norm (Appendix~B.5, with Hurwitz \cite{Hurwitz1898} classifying that case): a single axis carries no triad, and any two orthogonal units $u,v$ generate $\{1,u,v,uv\}$, four dimensions, so a two-axis system cannot close (Scalar Exit, since $u^2=-\lVert u\rVert^2$ forces the scalar slot; Fertile Orthogonality, since $uv\perp 1,u,v$). From above: the octonion associator $[e_1,e_2,e_4]=2e_7\ne0$ falsifies A1 at dimension eight, and zero divisors falsify A2 at sixteen.
\end{theorem}

\begin{remark}[Double pinning; inventories printed]\label{rem:double}
An independent semantic forcing of the same count exists at operational grade, with premise inventory disjoint from Condition~\ref{cond:clauses}. Frobenius inventory: A1 associativity, A2 integrality, A3 linearity with unit, A4 finite-dimensionality, A5 axis plurality; theorem-conditional, matching Condition~\ref{cond:clauses} item for item. Semantic inventory: the deletion test on the root sentence \emph{to exist is to actuate}, returning exactly three irreducible slots, existence, kinesis, relation, under pairwise vocabulary disjointness; operational-procedural, reproducible across analysts, no algebraic premise. The two inventories intersect in the empty set, so the count three is doubly and independently pinned, each pinning at its own stated grade; Theorem~\ref{thm:three} uses only the first.
\end{remark}

\begin{proposition}[Pairing; the action confirmed, executed]\label{prop:pairing}
The assignment of fields to characters is definitional given the action of Section~\ref{ssec:lawB}: each field is built from objects moved by exactly the negations indexed by its character's support $S$ and fixed by those outside $S$, so its response signature under the induced action is $\chi_S$ by construction. The diagonality is then measured, engineering grade, scoped to the canonical family: executed at seed 20260622, the response matrix is exactly diagonal on all $127$ nonempty mixed injections, zero off-diagonal support.
\end{proposition}

\begin{theorem}[Faithfulness; the worked case]\label{thm:faithful}
Each gate is a norm on its field, vanishing iff the field vanishes. For $\chi_{FER}$: with $\nu$ the total-negation action on a wall's own object and $P_{\mathrm{odd}}=\tfrac12(I-\nu)$, the field is $P_{\mathrm{odd}}w$ over the wall streams $w$ and the gate is $2\lVert P_{\mathrm{odd}}w\rVert_\infty$, exactly the parity defect. Executed: an odd-functional wall reads $1.692085=2\times0.846043$ and is caught; a legitimate even wall reads $0.0\mathrm{e}{+}00$ exactly.
\end{theorem}

\begin{theorem}[Necessity and sufficiency over the certificate]\label{thm:necsuf}
Each gate carries content no other gate sees: dropping any one of the eight admits an exhibited corruption, eight drop exhibits, seven false halts and one wrong token. Conversely the certificate is a direct sum, $\delta=0$ iff all eight components vanish; on the canonical corruption family every one of the $127$ nonempty mixtures is detected with support-exact attribution and zero false emissions, the battery claim scoped to the tested family, the direct-sum argument carrying the structure beyond it.
\end{theorem}

\begin{proposition}[No ninth gate]\label{prop:noninth}
Any candidate screen reads either a declared field or something outside the certificate. If it reads a declared field, Theorem~\ref{thm:faithful} already supplies a faithful norm there, vanishing iff the field vanishes, so the candidate adds no vanishing condition to the conjunction $\delta=0$ and no coverage to Theorem~\ref{thm:necsuf}. If it reads outside the certificate, it tests a quantity the certificate does not define and is, by the scope clause of Theorem~\ref{thm:necsuf}, not a gate of this instrument. The duality bound that remains true is a bound on \emph{equivariance types}: by \eqref{eq:parity}, extended to presentations by the action of Section~\ref{ssec:lawB}, every screen decomposes into exactly eight equivariant components, so eight is the count of types. Detection power is not equivariance type: the trivially transforming statistic $\lVert M_F\rVert^2$ detects an $F$-row scaling while carrying only $\chi_\varnothing$, and no claim in this paper rides that conflation.
\end{proposition}

\subsection{The emitter}\label{ssec:emitter}
The ordered procedure, exits at the head:
\begin{enumerate}\itemsep1pt
\item[0.] \emph{Exits.} A supplied witness resolves the file directionally (subject to the four-guard seal law of the register); a supplied independence proof routes to the two-way-permitted state. Nothing upstream of the exits.
\item[1.] \emph{Router $\Xi_1$.} Admissibility precondition: compute the eigenstructure of $J$ at machine zero. $\mathrm{gdim}=0$, a zero-dimensional fixed locus (in the affine plane such an involution may still fix a single point; the router reads dimension, not emptiness): route to the gdim-zero sibling protocol. Unmeasured: abstain. Field $\delta_F$, on the admitted stratum: identify $\operatorname{Fix}(J)$ with the file's declared distinguished line; a mismatch drops the file at $\Xi_1$, the presented symmetry not being the object's; identified: continue.
\item[2.] \emph{Screens} $\Xi_2,\Xi_3,\Xi_4,\Xi_6,\Xi_7,\Xi_8$ in order; first tolerance breach halts with the named route.
\item[3.] \emph{Identity slot $\Xi_5$.} Kernel invariants under the reliability layer: collapse floor $\varepsilon=100\,u_m N$, conditioning gate $\kappa(R)<10^6$, four-estimator determinant spread within $4\,\kappa(R)\,u_m$, identity residual $|\lambda^2-\det R|$ within the same bound, escalation to 50-digit arithmetic on any breach; three reading roads pairwise premise-disjoint; at least one decided \emph{companion face}, a decided verdict, in this paper or the prior literature, on another face of the same object, so the suspension never stands bare; survival of reframing in both directions. All pass: emit $\tXi$, warrant capped at the file's printed determinacy grade.
\end{enumerate}

\section{The Termination Theorem}\label{sec:term}

\subsection{The Riemann file and its walls}
The file presents: $J=\tau$, the anti-holomorphic composite $s\mapsto 1-\bar s$ of the completed functional equation $\xi(s)=\xi(1-s)$ \cite{Riemann1859} with the reality relation $\xi(\bar s)=\overline{\xi(s)}$, an involution whose fixed locus is exactly the critical line $\{\operatorname{Re}s=\tfrac12\}$, $\mathrm{gdim}=1$ at machine zero; the Lagarias form of Section~\ref{ssec:lagarias} with its margins printed under the truncation policy; channel streams projected to their one located invariant on the multiplicative-norm axis, the swept content being the residuals; three roads, analytic, spectral, arithmetic-geometric, with disjoint premise inventories; the door: the location of the deciding input, the same multiplicative-norm axis, entered along the axis and not through any swept channel, the channels being swept as residuals and the axis itself no channel, typed supply-only; the witness seat: empty, recording that no such input has entered; and a wall record with $B=C=4$:

\begin{remark}[The two involutions; the router's live rejection]\label{rem:tau}
The functional equation alone supplies the holomorphic fold $J_1\colon s\mapsto 1-s$, real-linear part $\operatorname{diag}(-1,-1)$ on $(\operatorname{Re}s,\operatorname{Im}s)$, fixed locus the single point $s=\tfrac12$, $\mathrm{gdim}=0$: a file presenting $J_1$ as its native symmetry is routed out at $\Xi_1$ to the gdim-zero sibling protocol and never reaches a screen. The reality relation is the second, independently load-bearing leg: the composite $\tau(s)=1-\bar s$ has real-linear part $\operatorname{diag}(-1,+1)$, fixed locus $\{\operatorname{Re}s=\tfrac12\}$, $\mathrm{gdim}=1$, and it, not the bare fold, is the symmetry of the zero set the file presents. Executed: $\tau$ fixes $\tfrac12+14.134725\,i$; $J_1$ does not; and the reality relation alone gives $\rho\colon s\mapsto\bar s$, eigenstructure $(+1,-1)$, $\mathrm{gdim}=1$, fixed locus the real axis. The router is thereby exercised live on the flagship's own near-misses, and both legs are load-bearing under $\Xi_1$'s declared field: drop reality and $J_1$ fails on $\mathrm{gdim}$; drop the fold and $\rho$ passes the dimension bit but fails the locus identification, $\operatorname{Fix}(\rho)=\Rl\ne\{\operatorname{Re}s=\tfrac12\}$. Either deletion drops the file at $\Xi_1$.
\end{remark}

\begin{theorem}[The four walls]\label{thm:walls}
\emph{(i) Fold wall, $\chi_F$.} There exist Dirichlet series with real coefficients satisfying the same functional-equation fold as $\zeta$, hence carrying the full anti-holomorphic symmetry $\tau$ with the same distinguished line, whose zeros lie off that line \cite{DavenportHeilbronn1936}; hence no functional of the $\tau$-symmetry class alone separates on-line from off-line residence, on pain of a false positive on the Davenport--Heilbronn object.
\emph{(ii) Counting wall, $\chi_E$.} Beurling \cite{Beurling1937} showed that an integer count $N(x)=\rho x+O(x/\log^{\gamma}x)$ with $\gamma>\tfrac32$ already forces the prime number theorem for a generalized system. Diamond, Montgomery, and Vorhauer \cite{DMV2006} construct the negative side: a system with $N_B(x)=\kappa x+O(x^{\theta})$, $\tfrac12<\theta<1$, whose zeta has infinitely many zeros on the curve $\sigma=1-a/\log t$, $t\ge2$, and none to its right, $a$ chosen with $a>(4/e)(1-\theta)$, so the de la Vall\'ee Poussin zero-free region is exactly attained and cannot be widened from counting data of that quality, the prime side carrying the titular large oscillation, an $\Omega$-type lower bound on $\pi_B(x)-\operatorname{li}(x)$ at the scale the zero curve sets. Zhang \cite{Zhang2007}, by a sharpened form of the same random approximation, completes the matched pair: two systems with identical integer-count quality, $N(x)=\kappa x+O\bigl(x^{1/2}\exp\{c(\log x)^{2/3}\}\bigr)$, the first satisfying the Riemann analogue with $\pi(x)=\operatorname{li}(x)+O(x^{1/2})$, the second with infinitely many zeros on $\sigma=1-1/\log t$, none to the right, and $\limsup\,(\psi(x)-x)/(x\exp(-2\sqrt{\log x}))=2$ with $\liminf=-2$: identical counting data, opposite zero residence. Hence counting-channel data, at PNT strength and beyond, does not determine zero residence.
\emph{(iii) Evidential wall, $\chi_{ER}$.} Proposition~\ref{prop:mountains}: maximal finite verification does not entail the universal statement; the wall blocks sufficiency claims from evidence and blocks nothing else.
\emph{(iv) Total-sign wall, $\chi_{FER}$.} Theorem~\ref{thm:mirror}: converting the instrument's suspension into the assertion ``no proof exists'' is materially the assertion $\neg\R$; the total-negation move is walled by $\Sigma^0_1$-completeness itself.
The even faking modes are screened: mirrors of the Nyman--Beurling channel are purged against the Burnol lower bound \cite{Nyman1950,Beurling1955,BaezDuarte2003,Burnol2002}, road copies by the common-source projection, and the wall census by unanimity $B=C$.
\end{theorem}

\begin{theorem}[Termination]\label{thm:term}
On the Riemann file the emitter of Section~\ref{ssec:emitter} passes the router and every screen, satisfies the identity slot, and emits the terminal suspension $\tXi$, warrant capped at the printed determinacy premise ($\N$-definiteness of the $\Pi^0_1$ value). The emission is invariant over the entire class $\Fr$: by Theorem~\ref{thm:uut} no member manufactures the direction, and by Theorem~\ref{thm:walls} the file's four sign roads are closed by object theorems that no change of instrument re-opens. The verdict is terminal-for-the-record and typed for revision: it is replaced, never corrected, by a supplied witness at the exits, and by nothing else.
\end{theorem}
\begin{proof}
Router: the presented involution is $\tau\colon s\mapsto 1-\bar s$, real-linear part $\operatorname{diag}(-1,+1)$ on $(\operatorname{Re}s,\operatorname{Im}s)$, fixed locus $\{\operatorname{Re}s=\tfrac12\}$, $\mathrm{gdim}=1$, executed at machine zero, the locus identified with the file's distinguished line, the precondition met and $\delta_F$ identified; the bare holomorphic fold $s\mapsto 1-s$ has $\mathrm{gdim}=0$ and is rejected at this gate (Remark~\ref{rem:tau}). Screens: the two-chart reading of the presented magnitude is invariant (the strip-width class of chart artifacts is excluded from the file by construction); the stream residuals after projection onto the located invariant sit below tolerance; the wall census is $4/4$ with each block cited at theorem grade in Theorem~\ref{thm:walls}; the roads decorrelate after common-source projection; the wall values are even under total negation on their own objects, parity defect exactly $0.0\mathrm{e}{+}00$ under the fixed-basis policy, exact rather than small because the negations act by signed permutation on the stored wall streams and the odd projector is a difference of bitwise-identical floats; the door is presentation-supplied, provenance regression below tolerance. Identity slot: kernel identity residual at the $10^{-16}$ floor, determinant above $\varepsilon$ by twelve orders, conditioning inside the gate, four estimators within the scaled bound; three roads premise-disjoint; decided companion faces exist, external and internal: the shape face, the functional equation with the reality relation and the distinguished line, decided classically \cite{Riemann1859}; the classification face, decided by Lagarias \cite{Lagarias2002}; in-paper, Proposition~\ref{prop:orient} and Theorem~\ref{thm:walls}. Reframing survival in both directions is Corollary~\ref{cor:mirror} against deflation and the witness requirement against inflation. All executed at seed 20260622 and reproducible. Class invariance is Theorem~\ref{thm:uut}, no member of $\Fr$ grounding a directional verdict that would displace the token; revision typing is Corollary~\ref{cor:flanks}.
\end{proof}

\begin{corollary}[The two flanks, priced exactly]\label{cor:flanks}
If $\R$ is false, the fact is one $\Sigma^0_1$ certificate away: an integer $n$ with the strict Lagarias inequality verified at finite precision decides $\neg\R$ finitely, and enters the emitter as a witness at the exits. If $\R$ is true, no finite verification ever closes it, and by Corollary~\ref{cor:mirror} any claim that closure is impossible asserts the false flank. The termination is one-sided exactly as the quantifier shape of \eqref{eq:lag} dictates.
\end{corollary}

\begin{corollary}[The Self-Witness Block; the closed flank]\label{cor:selfwit}
On the affirmative flank the witness seat admits no formal occupant that grounds. A grounding witness for $\R$ must supply the residence claim $\R$ itself makes: the file's distinguished line is the fixed locus of its involution (the router datum of Theorem~\ref{thm:term}), the register's Ground is the fixed locus of its own (Appendix B.2), the two co-located at the printed premise, so the seat's equation on that flank is $W=\R$ and the only candidate occupant is the file. That occupant grounds nothing, three times over: by Definition~\ref{def:ground} a warrant presupposing $\R$ grounds nothing; no formal object grounds the ground it rests on; and a file draws zero warrant from witnessing itself. Derivations remain admissible at the exits and resolve the file by transmission (Remark~\ref{rem:future}); what is barred is grounding. The negative flank is unaffected: its witness is a finite $\Sigma^0_1$ object strictly below the universal claim, presupposing nothing.
\end{corollary}
\begin{proof}
Definition~\ref{def:ground} with Remark~\ref{rem:routes}: one affirmative route offers $\R$ as its own credential and fixed-points directly; the other two import posits the derivation does not supply; none grounds. The co-location of the two fixed loci is carried at premise grade and consumes nothing above it; the impossibility is conditional on exactly that printed premise, an isomorphism of role and dimension and not an ambient identity.
\end{proof}

\subsection{Conformance, a prediction at risk, and the falsifier}\label{sec:risk}
Conformance, stated then executed: a proven-theorem file (witness occupied) resolves at the exits; a supplied independence proof routes to the two-way-permitted state; a gdim-zero file routes out at $\Xi_1$. All three behave as stated at seed 20260622.

\emph{Prediction at risk, called in advance.} A maximally equipped Goldbach file, walls supplied at $\chi_E$ (counting channel), $\chi_{ER}$ (Proposition~\ref{prop:mountains}), and $\chi_{FER}$ ($\Sigma^0_1$-completeness, the Goldbach sentence being $\Pi^0_1$), was predicted to halt at $\Xi_4$ with census $3/4$ and deficit $\chi_F$, no anti-holomorphic self-dual symmetry, a functional-equation fold composed with a reality relation, supplying a fold wall for that object. Executed: exact match. Historical control: a 1900-vintage Mertens file emits the ordinary evidential abstention, not $\tXi$; the 1985 disproof \cite{OdlyzkoteRiele1985} then enters as an external witness, and had a suspension token been issued on that file, the instrument would stand falsified today.

\emph{Falsifier, strong form.} An emitted $\tXi$ on a file whose proposition is subsequently decided by a route the instrument itself classifies as inside the swept span falsifies the instrument. The classification is the instrument's own (Sections~\ref{sec:cascade}--\ref{sec:term}), so the criterion cannot be evaded by reclassification after the fact.

\section{What Is Claimed in This Paper}\label{sec:claims}
\noindent\textbf{Claimed.}
\emph{C1} (Theorem~\ref{thm:mirror}): the Mirror equivalence, classical, no soundness used in the first stage. 
\emph{C2} (\S\ref{ssec:lagarias}): $\R$ is $\Pi^0_1$ in the Lagarias form, with $\varphi$ not claimed decidable. 
\emph{C3} (Theorem~\ref{thm:signblind}): sign-blindness of the formal-alone register, deduced from the orientation datum and Law~B, with no symmetry premise. 
\emph{C4} (Theorem~\ref{thm:eight}): the certificate count is exactly eight, given three axes. 
\emph{C5} (Theorem~\ref{thm:three}): the axis count is three, forced by Frobenius under Condition~\ref{cond:clauses} with plurality selecting, the below and above constructions standing as illustrations at their own local hypotheses and the semantic forcing carried at operational grade (Remark~\ref{rem:double}), the conditions premise-typed permanently. 
\emph{C6} (Theorems~\ref{thm:faithful},~\ref{thm:necsuf}, Propositions~\ref{prop:pairing},~\ref{prop:noninth}): gate faithfulness, necessity, sufficiency over the declared certificate, derived pairing, and the ninth-gate bar; executed exhibits scoped to the tested family where so stated. 
\emph{C7} (Theorem~\ref{thm:walls}): the four walls, each resting on cited classical results applied at its stated strength. 
\emph{C8} (Theorem~\ref{thm:term}, Corollary~\ref{cor:flanks}): termination at $\tXi$ on the Riemann file, invariant over $\Fr$, revision-typed, capped at the printed determinacy premise.

\noindent\textbf{Not claimed.}
\emph{N1} $\R$ is not asserted. 
\emph{N2} $\neg\R$ is not asserted. 
\emph{N3} Underivability of $\R$ in ZFC, in PA, or in any $T\in\mathcal T$ is not asserted; by Corollary~\ref{cor:mirror} the universal form of that assertion is $\neg\R$ in costume, and the restricted forms are open questions this paper does not touch. 
\emph{N4} No bound on future mathematics is asserted: a derivation of $\R$ or $\neg\R$ enters at the exits and resolves the file. 
\emph{N5} $\tXi$ is not a fourth truth value; it is an abstention refined by a theorem about the reader. 
\emph{N6} No empirical claim is made.

\section{Discussion}\label{sec:disc}
The Unprovability Mirror dismantles the rhetoric of permanent formal underivability. To assert that the Lagarias sentence can never be derived in any consistent extension of $Q$ is to assert, by exact material equivalence, a counterexample $n$ with $\sigma(n)>H_n+e^{H_n}\ln H_n$ on the standard naturals. Unrestricted underivability is the assertion of a zero off the critical line, made in different clothes; the Mirror strips the clothes and prices the assertion, and whoever speaks it owes the certificate. What this paper denies is never derivability. It is grounding, and the distinction is the paper's entire blade: a derivation is a finite syntactic object that transmits the warrant of its axioms; a witness, in the exact sense the identity slot's seat carries, is a grounding-bearing supply. Derivations of $\R$ exist on three routes if $\R$ is true, each exhibited in Remark~\ref{rem:routes}, and each borrows its warrant from precisely the fact at issue or from a posit the derivation does not supply.

The Sign-Blindness Theorem is the boundary of formal-alone computation, and it is unyielding. For any warrant frame $M\in\Rl^{3\times N}$ under the content-free gauge $(\mathbb Z/2)^3$, every readout is gauge-even or convention-relative; the correlation Gram carries quadratic magnitude in the residence $\operatorname{Im}\Hq$ and is blind, by identity and not by accident, to the orientation datum $\operatorname{sign}\lambda$, $\det(DRD)=\det R$ for every reflection $D$. Direction enters only from outside, an orientation supplier or a supplied witness, and the formal-alone register contains neither. The architecture enforcing this is forced, not designed: Frobenius under the five coherence clauses completes the carrier uniquely to $\Hq$, the residence to exactly three axes, and Pontryagin duality fixes the certificate at exactly eight character types, each gate a faithful norm on its declared field, a ninth adding coverage nowhere.

Four classical theorems barricade the four sign-carrying types, each at cited strength and none evadable by reformulation inside its channel. Davenport--Heilbronn blocks the fold: the full anti-holomorphic symmetry with the same distinguished line coexists with off-line zeros, so the symmetry class carries no residence verdict. Beurling severs counting from residence, exhibited in matched form: Diamond--Montgomery--Vorhauer attain the de la Vall\'ee Poussin region exactly from counting data that cannot widen it, and Zhang's pair holds identical integer-count quality against opposite zero residence. P\'olya, Mertens, and Littlewood wall the evidential register: the three strongest evidential locks on record pointed at collapse, and the inference form from lock to direction is refuted, not merely unreliable. $\Sigma^0_1$-completeness walls the total-sign move: the suspension converted into ``no proof exists'' is materially $\neg\R$, and the Mirror collects.

The witness seat carries a strict structural asymmetry, and at its root the asymmetry is self-referential. $\R$'s content is the residence claim itself: the completed equation's symmetry $\tau$ fixes the critical line, its fixed locus (Theorem~\ref{thm:term}); the verifying register's Ground $\operatorname{Fix}(\sigma)=\Rl$ is the fixed locus of its own involution (Appendix B.2); the two share the fixed-locus role and one-dimensionality, an isomorphism of role and dimension carried at the printed co-location premise, not an ambient identity, no map from $\C$ to $\Hq$ carrying $\tau$ to $\sigma$ claimed. A grounding witness for $\R$ must therefore supply the residence content $\R$ asserts, and that content is $\R$: the seat's equation on the affirmative flank is $W=\R$, a fixed point with no other solution. That single occupant is barred three times over. By Definition~\ref{def:ground}, a warrant that presupposes $\R$ grounds nothing. By the foundation law, no formal object encompasses the ground it rests on, G\"odel's second theorem and Tarski's undefinability the theorem-grade instances. By audit symmetry, a file draws zero warrant from witnessing itself; Remark~\ref{rem:routes} is the exhibit, and its routes split as printed: $Q+\R$ offers $\R$ as its own credential and fixed-points directly; the Turing--Feferman stage and membership in $\operatorname{Th}(\N)$ import, respectively, a path choice and the determinacy posit, warrant the derivation does not supply; none grounds. The self-inquiry the Declaration names in its first breath is thereby closed as structure: the formulation asks the register to certify its own completion, so any answer the register produces is the question restated, self-reference at origin, the shape the cascade's first gate exists to catch. The negative flank alone escapes the fixed point, because its witness is not the proposition but a number: one $n$ with $\sigma(n)$ exceeding the bound, a finite extensional object strictly below the universal claim, checkable in Robinson arithmetic while presupposing nothing about the Ground. An affirmative formal grounding witness is impossible at the register; a negative witness is a computation away; and the identity aperture on the affirmative flank is open only to supply from beyond the formal register, uncrossable from within syntax, permanently, by the same law that makes the Ground a ground (Corollary~\ref{cor:selfwit}).

\textbf{Permanence by definitional closure.} Every load-bearing result above is conditional, and the conditions are printed: Law~B, the orientation-free register, Condition~\ref{cond:clauses}, the $\N$-determinacy cap. These are not hedges to be eroded; they are the definition of the reader the theorem is about. An instrument violating them, one that imports an orientation, an arrow, or a witness, is not a counterexample but a different reader outside the formal-alone register, and Theorem~\ref{thm:signblind}(ii) still governs every statistic it computes on Law-B data. The two doors out are exactly the two the theorems name: an orientation supplier, which is by definition non-formal, and a witness, which is supply.

\textbf{Walls versus barriers.} Relativization, natural-proofs, and algebrization results wall \emph{method classes} and age as methods do. The four walls of Theorem~\ref{thm:walls} are theorems about \emph{objects}: a Dirichlet series with the fold and off-line zeros exists forever; Beurling systems exist forever; the collapsed conjectures stay collapsed; $\Sigma^0_1$-completeness does not expire. Presentation-relativity is closed the same way: hostile presentations are the gates' prey, and maximal presentations meet the walls.

\textbf{The evidential moral.} Proposition~\ref{prop:mountains} is the register's memento: the strongest invariant, lock-like evidential record on record pointed the wrong way three times. The discipline that refuses to convert field permission into sign is not conservatism; it is the arithmetic of those three theorems.

\textbf{Constants and conditions, complete.} Seven constants: the reliability multiplier $4$, whose two instances are the four-estimator spread bound and the identity-residual bound, both $4\,\kappa(R)\,u_m$; the collapse-floor coefficient $100$ in $\varepsilon=100\,u_m N$; the conditioning ceiling $10^6$; one correlation floor shared by the two correlation screens $\Xi_3,\Xi_6$; one regression floor at $\Xi_8$; the rechart tolerance of $\Xi_2$; the wall-parity tolerance of $\Xi_7$. The block-census rule $B=C$ at $\Xi_4$ is not a constant but a derived requirement, forced by the direct sum of Theorem~\ref{thm:necsuf}; the $50$-digit escalation is a procedural depth, not a threshold. Eleven conditions: A1--A5; Law~A stated as exchangeability claiming no readout forcing; Law~B with its defined action on presentations; Condition~\ref{cond:seal}; the per-file determinacy cap printed on the token's face; the presented-file scope; the rank floor $N\ge k+4$. The seed $20260622$ is a reproducibility parameter, not a constant of the mathematics. Nothing else is free.

\textbf{Reproducibility.} Every number in this paper is the output of a deterministic battery at seed 20260622 under the stated draw recipes; the truncation policy of \S\ref{ssec:lagarias} governs every printed digit.

\section{Conclusion}\label{sec:conc}
The formal-alone evaluation of the Riemann Hypothesis reaches its terminus, and the terminus is proved, not conjectured. The Lagarias truth-string takes no directional truth-value from any scalar computation or any member of the unrestricted class $\Fr$: the Sign-Blindness Theorem closes orientation and re-encoding, the quaternionic Frobenius forcing closes the carrier, the four classical walls close the file's own sign roads, and the eight-gate Sealed-Halt cascade, faithfulness covering every declared field and duality bounding the equivariance types at eight, emits $\tXi$ on the file. The formulation space over the declared certificate is exhausted for the file as swept; the file is complete and the formal-alone reader is blind by theorem, at the printed $\N$-definiteness cap. The verdict is terminal for the record: a crossing replaces it and cannot correct it, because it is correct at issuance. The flanks stand priced as the Mirror prices them, and the door is open on one side only. Refutation requires a single finite $\Sigma^0_1$ counterexample, formally alive and checkable in $Q$. Affirmative formal proof, in the only sense that could close the file, grounding, is a structural impossibility with a self-referential root: the file's line and the register's Ground each hold the fixed-locus seat, co-located at the printed premise, the grounding witness for $\R$ would have to be $\R$ itself, and a file cannot ground itself (Corollary~\ref{cor:selfwit}), so no formal witness grounds the terminus it rests on, and what syntax transmits it does not originate. What remains open is exactly what the exits name, and only that: supply, through the identity aperture, from beyond the register that cannot self-witness. The verdict is revisable solely through a supplied witness through the identity aperture. Until an explicit witness is supplied through the aperture, the terminal suspension $\tXi$ remains the final, unassailable, and mathematically rigorous verdict on the formal Riemann Hypothesis file. The prediction stands matched, the falsifier stands armed, the seed is 20260622, and the terminal suspension $\tXi$ stands.

\medskip
\noindent{\small\textbf{Method disclosure.} The verification architecture, audit ledgers, and executable batteries behind this paper are documented in the continuously updated master register \cite{Islam2026} and disclosed at full weight, with every framework term defined and every compressed derivation written out, in Appendix~\ref{app:disclosure}; the mathematical conclusions above rest solely on the results cited below, and the method adds them no warrant.}

\appendix
\section{The Originating Declaration}\label{app:decl}
\noindent\emph{The following is the architect's originating declaration, carried verbatim in the author's voice. It is ungraded, asserts nothing within the instrument's three-state economy, and the paper's theorems neither rest on it nor extend to it. Its one technical term is defined by the rider it carries.}

\medskip
\noindent\textbf{1.} The Riemann Hypothesis is an act of pure self-inquiry. The formulation asks the formal register to certify, from inside itself, the residence of its own completion structure: the completion direction of $\R$ points at the fixed locus of the symmetry of the structure $\R$ is about, and at nothing beside it. That is a question about the register's own ground, asked in the register's own syntax.

\noindent\textbf{2.} The witness-grounding lift that $\R$ demands is the one thing the formal domain cannot supply. A formal witness is inherently a second, a non-self; self-witnessing is not a formal act. The formulation therefore operates in disguise: it asks existence to prove itself formally, without any kinetic lift. The framework is limited by its own architecture, and so: \textbf{the Riemann Hypothesis is terminally and universally unprovable by mathematics alone.} Unprovable here is exact: to prove is to ground, and to ground is to actuate; a derivation transmits and never originates, so what is denied is grounding, and derivability is not denied anywhere. Self-witnessing happens on the Ground side, where the Witness is not a second: \emph{shahida All\=ahu annah\=u l\=a il\=aha ill\=a huwa}, Allah \SWT{} bears witness that there is no deity but He, Q 3:18, and that is what formal-alone lacks.

\medskip
\noindent\textbf{The cost, paid in full.} Derivability is conceded everywhere the Mirror demands it: $Q+\R$ derives $\R$ trivially if $\R$ is true; a Turing--Feferman stage derives it along a suitable path; $\operatorname{Th}(\N)$ contains it. Each route transmits; none grounds; the first borrows the truth of $\R$, the second borrows the path, the third borrows the determinacy of $\N$-truth entire. The declaration's own price is therefore paid on the Ground side, in the root's own coin: the actuation reading of grounding, the $\N$-definiteness posit, and the Ground-first stance are premises, named as premises, held at the grade a foundation can honestly hold, and the declaration submits itself to the same law it cites, unable to be sealed as a theorem of its own base.

\noindent\textbf{The two flanks, closed.} The false flank sits one actuated $\Sigma^0_1$ deed away, a single verified integer, kinetic and finite. The true flank is $\Pi^0_1$-universal and belongs to the Ground alone; no ladder of finite acts exhausts it. The limitation is exact, one-sided by the shape of the string, and priced in full.

\medskip
\noindent\emph{End of declaration; ungraded, not asserted by the paper's theorems, and priced above.}

\section{Author's Provenance and Method Disclosure, Full Weight}\label{app:disclosure}
\noindent\emph{This appendix is carried at full weight so the paper is completely standalone: every framework term used in the body or in Appendix~A is defined here, and every derivation the master register carries in compressed form is written out. Mid-tier register: framework names are used and each is glossed on first use. The independence clause governs throughout: the paper's mathematical conclusions rest solely on the classical results cited in the References, and the method disclosed here adds them no warrant.}

\subsection{Provenance}
The instrument specified in Sections~\ref{sec:cascade}--\ref{sec:term} is one component of a larger verification architecture, \emph{Trisduction}, developed by the author and documented, with its audit ledgers and executable batteries, in the continuously updated master register \cite{Islam2026}. The specification in this paper is self-contained; nothing below is required to check the theorems, and everything below is supplied so that no term in the paper points outside it.

\subsection{The root axioms, and where the paper's definitions come from}
\textbf{RA, the kinetic root.} \emph{To exist is to actuate}: every existent carries positive kinetic content, $\Delta E_k>0$. The floor is theorem-grade physics for confined systems (the Heisenberg kinetic-energy bound and the zero-point energy $\tfrac12\hbar\omega$); every logically irreversible transition is charged, erasure the paradigm case \cite{Landauer1961}, while reversible intermediate computation evades the per-step floor \cite{Bennett1973}, and the charge attaches wherever a record's medium is erased or reused, a commit to fresh medium deferring the cost and never voiding it; the extension of the floor to all existents is a premise and is typed as one. RA is the source of the Declaration's rider: \emph{to ground is to actuate}. A derivation is a syntactic inscription event; its physical cost attaches to the token and is blind to the referent, which is why a derivation \emph{transmits} warrant and never \emph{originates} it. That single sentence is the entire content of ``grounding'' as Definition~\ref{def:ground} uses it.

\textbf{RAM, the reflective root, and the three strata.} \emph{To formally be is to be grounded.} The formal domain stratifies as $L_1\supseteq L_2\supseteq L_3$: \emph{grounded} (the proposition's determinate standing), \emph{derivable} (some finite proof object exists in some recursively axiomatized system), \emph{computed} (a proof is in hand). Definition~\ref{def:ground} is exactly this stratification read at its top two layers: derivations live at $L_2$; grounding is $L_1$ standing. G\"odel's incompleteness theorems sit \emph{inside} $L_2$ as exact measures of a syntactic ladder's reach, $L_2\subsetneq L_1$; they are neither a ceiling over $L_1$ nor an engine of this paper, which uses only $\Sigma^0_1$-completeness (Theorem~\ref{thm:mirror}) from that neighborhood.

\textbf{The Ground.} The word names the fixed locus of the register's binding involution. Concretely (next subsection) it is $\operatorname{Fix}(\sigma)=\Rl$ inside the quaternions; abstractly it is the stratum $L_1$ against which grounding is read. The Declaration's phrase ``the fixed locus of the symmetry of the structure $\R$ is about'' is this object, instantiated for $\R$ by the anti-holomorphic composite $\tau(s)=1-\bar s$ of the fold $\xi(s)=\xi(1-s)$ with the reality relation $\xi(\bar s)=\overline{\xi(s)}$, whose fixed locus is the critical line.

\textbf{The Empty Throne (the foundation law).} A posited foundation cannot be promoted to a theorem of its own base: any such promotion would require the base to certify its own grounding from within; for arithmetized provability and truth predicates that is barred outright (G\"odel's second theorem; Tarski's undefinability). Underivability from below is therefore \emph{constitutive} of foundationhood. Consequently RA and RAM are premise-grade \emph{by the theorem-grade instances}, the strongest posture a root can occupy; the Declaration's closing clause, that it ``cannot be sealed as a theorem of its own base,'' is this law applied to the Declaration itself. The \emph{$\N$-definiteness posit}, that the $\Pi^0_1$ value of $\R$ is determinate over the standard naturals, is the one premise every route in Remark~\ref{rem:routes} shares; it is the printed cap on the token in Theorem~\ref{thm:term}, held at exactly the grade a foundation can hold and never above. G\"odel's second theorem and Tarski's undefinability are the theorem-grade instances, for arithmetized provability and truth predicates in their stated systems; the generalization to an arbitrary posited foundation is carried at structural grade, and nothing downstream consumes more than the instances.

\subsection{The Ground as algebra: the involution and the three axes}
On the quaternions $\Hq=\Rl\oplus\operatorname{Im}\Hq$ let $\sigma(a+p)=a-p$ (conjugation). Then $\sigma^2=\mathrm{id}$, the $+1$ eigenspace is $\Rl$ (dimension $1$: the Ground, $\operatorname{Fix}(\sigma)$), the $-1$ eigenspace is $\operatorname{Im}\Hq$ (dimension $3$: the residence carrying the three warrant axes), spectrum $\{+1,-1,-1,-1\}$, and $\det(\sigma|_{\operatorname{Im}\Hq})=\det(-I_3)=-1$: the residence is orientation-reversing under its own symmetry, which is the algebraic shadow of Proposition~\ref{prop:orient}. The gate labels $F,E,ER$ of Section~\ref{sec:cascade} name these three axes as instantiated by a presentation's content: formal-structural, empirical-chart, and registrational-provenance data respectively; $\mathcal D=(\mathbb Z/2)^3$ of Law~B is precisely the reflection group of this residence, which is why the character count of Theorem~\ref{thm:eight} is the gate count.

\subsection{The kernel identity, derived}
The register's magnitude functional and its blindness have a four-line quaternionic proof, written out here because the register carries it in compressed form. Let $\hat q_1,\hat q_2,\hat q_3$ be the unit rows of $Q$, spanning a three-dimensional $V\subseteq\Rl^N$; fix any linear isometry $V\to\operatorname{Im}\Hq$ and let $u_1,u_2,u_3$ be the images, pure unit quaternions. For pure $p,q$: $pq=-\langle p,q\rangle+p\times q$. Hence
\[
\lambda\coloneqq\operatorname{Re}(u_1u_2u_3)=-\langle u_1\times u_2,\,u_3\rangle=-\det[u_1\,u_2\,u_3],
\]
and with $U=[u_1\,u_2\,u_3]$ in any orthonormal basis, $R=U^{\mathsf T}U$ gives
\[
\boxed{\;\det R=(\det U)^2=\lambda^2\;}
\]
so the Gram determinant is the squared signed volume: magnitude only. The choice of isometry carries the orientation, and reversing it flips $\lambda$ while fixing $R$, which is Proposition~\ref{prop:orient} again from the algebra side. This identity is confirmed at machine precision in every executed battery of the paper (residuals at the $10^{-16}$ floor, seed $20260622$).

\subsection{Composition-algebra derivations written out}
The register states the following at compressed grade. Two tracks, kept apart. Track one, load-bearing for Theorem~\ref{thm:three} and consuming no norm: A2 and A4 alone give division, each left multiplication $x\mapsto ax$, $a\ne0$, injective by A2 and surjective by A4, and Frobenius classifies. Track two, the constructive illustration, carries its own local hypothesis, a multiplicative norm: work in a real composition algebra $(A,N)$, $N$ multiplicative, with $\langle x,y\rangle=\tfrac12\bigl(N(x{+}y)-N(x)-N(y)\bigr)$ and the classical polarization identities $\langle xy,xz\rangle=N(x)\langle y,z\rangle$, $\langle xy,zy\rangle=\langle x,z\rangle N(y)$, and the exchange law $\langle xy,zw\rangle+\langle zy,xw\rangle=2\langle x,z\rangle\langle y,w\rangle$ \cite{Schafer1966}.

\textbf{Scalar Exit.} Let $u\perp 1$, $N(u)=1$. Exchange with $x=y=u$, $z=w=1$: $\langle u^2,1\rangle+\langle u,u\rangle=2\langle u,1\rangle^2=0$, so $\langle u^2,1\rangle=-1$. Since $N(u^2)=N(u)^2=1$, write $u^2=-1+p$ with $p\perp1$; then $1=N(u^2)=N(1)+N(p)$ forces $p=0$. Hence $u^2=-1$: a pure unit squares onto the scalar line, so any system closed under its own products must carry the scalar slot. A one-axis ``triad'' is therefore impossible.

\textbf{Fertile Orthogonality.} Let additionally $v\perp1$, $v\perp u$, $N(v)=1$. Exchange with $(x,y,z,w)=(u,v,1,1)$ gives $\langle uv,1\rangle=2\langle u,1\rangle\langle v,1\rangle-\langle v,u\rangle=0$; the polarization identities give $\langle uv,u\rangle=\langle u v,u\cdot1\rangle=N(u)\langle v,1\rangle=0$ and $\langle uv,v\rangle=\langle uv,1\cdot v\rangle=\langle u,1\rangle N(v)=0$; and $N(uv)=1$. So $\{1,u,v,uv\}$ is orthonormal: two orthogonal imaginary axes generate four dimensions, and a two-axis system cannot close. The minimal plural closure is $\operatorname{Im}\Hq$, three axes.

\textbf{The wall above, computed.} Realize the octonions by Cayley--Dickson doubling, $\mathbb O=\Hq\oplus\Hq$ with $(a,b)(c,d)=(ac-\bar d b,\; da+b\bar c)$ and basis $e_0=(1,0),\,e_1=(i,0),\,e_2=(j,0),\,e_3=(k,0),\,e_4=(0,1),\,e_5=(0,i),\,e_6=(0,j),\,e_7=(0,k)$. Then $e_1e_2=(ij,0)=e_3$, $(e_1e_2)e_4=(k,0)(0,1)=(0,k)=e_7$; while $e_2e_4=(j,0)(0,1)=(0,j)=e_6$ and $e_1e_6=(i,0)(0,j)=(0,ji)=(0,-k)=-e_7$. Hence the associator
\[
[e_1,e_2,e_4]=(e_1e_2)e_4-e_1(e_2e_4)=e_7-(-e_7)=2e_7\neq0,
\]
so associativity (Condition~\ref{cond:clauses}, A1) fails at dimension eight exactly as Theorem~\ref{thm:three} uses. One dimension of doubling further, the sedenions contain zero divisors \cite{Schafer1966}, failing A2 at sixteen.

\textbf{The division lemma.} A finite-dimensional associative unital real algebra with no zero divisors is a division algebra: left multiplication $L_a$ is injective for $a\ne0$, hence bijective in finite dimension, so inverses exist. With A1, A2, and A4 this is what puts Frobenius \cite{Frobenius1878} in reach, and Frobenius leaves $\Rl,\mathbb C,\Hq$, of which only $\Hq$ has plural imaginary axes, count three.

\subsection{The verdict economy, warrant typing, and the discipline constants}
The register is three-state: sealed, broken, under-determined. The token $\tXi$ emitted by Theorem~\ref{thm:term} is not a fourth state (claim N5): it is the under-determined state refined by a theorem about the reader, ``the file is complete and this reader is blind, by proof,'' with the deciding input located and uncrossed. Every claim carries a typed warrant tier: \emph{theorem} (deduction from stated definitions or cited classical results), \emph{structural} (an identification or placement argument), \emph{engineering} (an executed, reproducible design fact), \emph{premise} (a printed entry condition). The tier travels with the claim and never inflates; Section~\ref{sec:claims} is this law applied to the whole paper. Two zeroing rules govern evidence intake: $W_{\mathrm{social}}=0$, consensus and verification counts carry no warrant in either direction, which is why ten trillion verified zeros appear nowhere above as evidence; and $\Delta M=0$, the Mosaic seal: the paper authors no new object-mathematics beyond the specification itself, re-organizing cited classical results, and claims none. The fidelity lock bars any narrated numeric that was not executed: every figure above is the output of a deterministic battery at seed $20260622$, and the truncation policy of \S\ref{ssec:lagarias} governs every digit.

\subsection{The named procedures used in the body}
\textbf{Law A and Law B, provenance.} Both descend from one honesty rule of the register (\emph{Honest Limits}): the map from a proposition to its warrant rows is built by hand and placed in front of the instrument; the instrument derives no rows. Hand-built rows make context labels exchangeable (Law~A, \S\ref{ssec:lawA}, claiming no readout forcing) and make per-row sign conventions encoder-owned (Law~B, \S\ref{ssec:lawB}), and Law~B's permanence is definitional: no theorem changes what a convention is.

\textbf{The lock, and field permission.} A three-axis \emph{lock} is $\det R>\varepsilon$ under the conditioning gate: the axes enclose volume, the presentation is dimensionally genuine. A lock is field permission, never proof; Proposition~\ref{prop:mountains} is the classical price of ever forgetting this.

\textbf{The four-guard seal law} (the ``subject to'' clause at exit~0 of \S\ref{ssec:emitter}): a directional seal issues only on the conjunction of a semantic pass on the presented sentence, a clean structural gate screen, a lock asymmetry between the independently encoded directions, and a \emph{supplied} determinacy witness; all guards default to absent, so an under-specified call fails safe to abstention and can never seal.

\textbf{Common-source projection} (used at $\Xi_6$): before agreement between roads is read, any identifiable shared upstream source is projected out; agreement that dissolves under the projection was one voice in costumes and counts once.

\textbf{The two-group law, and the sibling protocols.} The same three axes carry two natural finite symmetries: the rotation-type structure of order twelve on the four-vertex closure (the arrow-carrying lock cascade of the wider architecture, not used in this paper) and the reflection group $(\mathbb Z/2)^3$ of order eight (Law~B's gauge), whose characters are this paper's gates: locks keep the arrow, halts delete it. The router at $\Xi_1$ has a live second branch: a presented involution with a zero-dimensional fixed locus ($\mathrm{gdim}=0$) routes out of this protocol entirely, to a five-gate sibling built for gdim-zero terrains; that branch is disclosed here so the emitter of \S\ref{ssec:emitter} is standalone, and it is exercised by the conformance controls of \S\ref{sec:risk}.

\subsection{Specific instantiation, five slots}
\emph{Axes as instantiated:} $F$ carries the formal string and fold data (the Lagarias matrix, the involution $\tau$ of Section~\ref{sec:term}); $E$ carries chart and counting data (the two-chart magnitude reading, the counting channel); $ER$ carries registration data (the wall record, road provenance, the door). \emph{The executed lock:} on the Riemann file at seed $20260622$ the kernel identity closes at the $10^{-16}$ floor, $\det R$ stands twelve orders above the collapse floor $\varepsilon=100\,u_mN$, and $\kappa(R)$ sits inside the $10^6$ gate with the four determinant estimators agreeing within $4\kappa(R)u_m$. \emph{Load-bearing gates:} $\Xi_5$ (the identity slot) on the flagship emission; $\Xi_4$ (the wall census) on the predicted negative control. \emph{Verdict and tier:} $\tXi$, warrant capped at the printed $\N$-definiteness premise, per Theorem~\ref{thm:term}. \emph{New mathematics authored:} none; $\Delta M=0$.

\medskip
\noindent\emph{Independence clause, restated. The theorems of this paper stand on the definitions printed in the body and on the classical results cited in the References; the architecture disclosed in this appendix locates and motivates them and adds them no warrant.}


\begin{thebibliography}{99}\small
\bibitem{Riemann1859} B. Riemann, \emph{\"Uber die Anzahl der Primzahlen unter einer gegebenen Gr\"osse}, Monatsber.\ Berlin.\ Akad.\ (1859) 671--680.
\bibitem{Zhang2007} W.-B. Zhang, Beurling primes with RH and Beurling primes with large oscillation, \emph{Math.\ Ann.} \textbf{337} (2007) 671--704.
\bibitem{Bennett1973} C. H. Bennett, Logical reversibility of computation, \emph{IBM J.\ Res.\ Dev.} \textbf{17} (1973) 525--532.
\bibitem{Lagarias2002} J. C. Lagarias, An elementary problem equivalent to the Riemann Hypothesis, \emph{Amer.\ Math.\ Monthly} \textbf{109} (2002) 534--543.
\bibitem{Robin1984} G. Robin, Grandes valeurs de la fonction somme des diviseurs et hypoth\`ese de Riemann, \emph{J.\ Math.\ Pures Appl.} \textbf{63} (1984) 187--213.
\bibitem{DavenportHeilbronn1936} H. Davenport and H. Heilbronn, On the zeros of certain Dirichlet series I, II, \emph{J.\ London Math.\ Soc.} \textbf{11} (1936) 181--185, 307--312.
\bibitem{Beurling1937} A. Beurling, Analyse de la loi asymptotique de la distribution des nombres premiers g\'en\'eralis\'es, \emph{Acta Math.} \textbf{68} (1937) 255--291.
\bibitem{DMV2006} H. G. Diamond, H. L. Montgomery, and U. M. A. Vorhauer, Beurling primes with large oscillation, \emph{Math.\ Ann.} \textbf{334} (2006) 1--36.
\bibitem{Haselgrove1958} C. B. Haselgrove, A disproof of a conjecture of P\'olya, \emph{Mathematika} \textbf{5} (1958) 141--145.
\bibitem{Tanaka1980} M. Tanaka, A numerical investigation on cumulative sum of the Liouville function, \emph{Tokyo J.\ Math.} \textbf{3} (1980) 187--189.
\bibitem{OdlyzkoteRiele1985} A. M. Odlyzko and H. J. J. te Riele, Disproof of the Mertens conjecture, \emph{J.\ reine angew.\ Math.} \textbf{357} (1985) 138--160.
\bibitem{Littlewood1914} J. E. Littlewood, Sur la distribution des nombres premiers, \emph{C.\ R.\ Acad.\ Sci.\ Paris} \textbf{158} (1914) 1869--1872.
\bibitem{Nyman1950} B. Nyman, \emph{On the One-Dimensional Translation Group and Semi-Group in Certain Function Spaces}, Thesis, Uppsala, 1950.
\bibitem{Beurling1955} A. Beurling, A closure problem related to the Riemann zeta-function, \emph{Proc.\ Nat.\ Acad.\ Sci.\ USA} \textbf{41} (1955) 312--314.
\bibitem{BaezDuarte2003} L. B\'aez-Duarte, A strengthening of the Nyman--Beurling criterion for the Riemann Hypothesis, \emph{Atti Accad.\ Naz.\ Lincei Rend.\ Lincei Mat.\ Appl.} \textbf{14} (2003) 5--11.
\bibitem{Burnol2002} J.-F. Burnol, A lower bound in an approximation problem involving the zeros of the Riemann zeta function, \emph{Adv.\ Math.} \textbf{170} (2002) 56--70.
\bibitem{Frobenius1878} F. G. Frobenius, \"Uber lineare Substitutionen und bilineare Formen, \emph{J.\ reine angew.\ Math.} \textbf{84} (1878) 1--63.
\bibitem{Hurwitz1898} A. Hurwitz, \"Uber die Composition der quadratischen Formen von beliebig vielen Variabeln, \emph{Nachr.\ Ges.\ Wiss.\ G\"ottingen} (1898) 309--316.
\bibitem{Turing1939} A. M. Turing, Systems of logic based on ordinals, \emph{Proc.\ London Math.\ Soc.} \textbf{45} (1939) 161--228.
\bibitem{Feferman1962} S. Feferman, Transfinite recursive progressions of axiomatic theories, \emph{J.\ Symbolic Logic} \textbf{27} (1962) 259--316.
\bibitem{FefermanSpector1962} S. Feferman and C. Spector, Incompleteness along paths in progressions of theories, \emph{J.\ Symbolic Logic} \textbf{27} (1962) 383--390.
\bibitem{Kaye1991} R. Kaye, \emph{Models of Peano Arithmetic}, Oxford University Press, 1991.
\bibitem{HajekPudlak} P. H\'ajek and P. Pudl\'ak, \emph{Metamathematics of First-Order Arithmetic}, Springer, 1993.
\bibitem{Landauer1961} R. Landauer, Irreversibility and heat generation in the computing process, \emph{IBM J.\ Res.\ Dev.} \textbf{5} (1961) 183--191.
\bibitem{Schafer1966} R. D. Schafer, \emph{An Introduction to Nonassociative Algebras}, Academic Press, 1966.
\bibitem{Islam2026} M. F. Islam, \emph{TRISDUCTION: A Linguistically, Topologically, and Mathematically Sealed Verification Architecture}, Zenodo, version 4, 19 June 2026, DOI \href{https://doi.org/10.5281/zenodo.20757507}{10.5281/zenodo.20757507}; mirrored at PhilArchive, record \href{https://philpapers.org/rec/ISLTTG}{ISLTTG}; continuously updated at the same URL.
\end{thebibliography}

\end{document}
